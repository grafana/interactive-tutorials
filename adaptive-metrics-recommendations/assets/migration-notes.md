---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: adaptive-metrics-recommendations

## Files Created

- `manifest.json` — standalone guide manifest generated from index.json rule match and content.json id/title

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `adaptive-metrics-recommendations` |
| `type` | Convention (standalone guide) | `"guide"` |
| `description` | `index.json` rule (verbatim) | `"Hands-on guide: Reduce metrics volume safely with Adaptive Metrics."` |
| `category` | Default (not inside a `*-lj` directory) | `"general"` |
| `author.team` | Convention (standalone guide) | `"interactive-learning"` |
| `author.name` | `git log --follow` (single non-bot author) | `"brendamuir"` |
| `startingLocation` | First URL-bearing leaf in `match.and` — `urlPrefixIn[0]` | `"/adaptive-telemetry"` |
| `targeting.match` | `index.json` rule (copied verbatim) | `{ "and": [{ "targetPlatform": "cloud" }, { "urlPrefixIn": [...] }] }` |
| `testEnvironment.tier` | Tier inference: `targetPlatform: "cloud"` present, no `source` | `"cloud"` |
| `depends` / `recommends` / `suggests` / `provides` | No data available | `[]` |
| `repository` | Omitted (schema default `"interactive-tutorials"` applies) | — |
| `language` | Omitted (schema default `"en"` applies) | — |

## Validator Result

`node dist/cli/cli/index.js validate --package adaptive-metrics-recommendations` — **PASS**

Only info-level defaults reported (repository, language, schemaVersion, conflicts, replaces) — all expected.

## Surprises / Notes

- The CLI was not pre-built in the pathfinder-app checkout; `npm run build:cli` was run first to compile it before validation.
- The index.json rule title (`"Reduce metrics volume using Adaptive Metrics recommendations"`) differs slightly from the content.json title (`"Reduce metric volume using Adaptive Metrics"`). Both refer to the same guide; no conflict action needed.

## TODO

- [ ] Review whether `recommends`, `suggests`, or `provides` should be populated once semantic relationships for the Adaptive Telemetry guide family are defined.
- [ ] Confirm `author.name: "brendamuir"` is the correct attribution (derived from git history).
