---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-08T00:00:00Z"
status: incomplete
---

# Migration Notes: welcome-to-play

## Structural Ambiguity

This directory is a **special case**: it contains 3 nested sub-guide directories (`main-page/`, `visualization-page/`, `datasource-page/`), but it is NOT a standard `-lj` learning path. It was detected as Mode 2 (nested content.json directories) and migrated as `type: "path"` with 3 milestones.

**Key structural questions for the reviewer:**

1. **Are these truly sequential milestones?** The three parts each target different Play dashboards and are independently targeted via `index.json`. They could be consumed independently rather than sequentially. The `depends` chain (main -> visualization -> datasource) was applied by convention but may not reflect actual user intent.

2. **Should this be a metapackage?** If these are 3 standalone guides that happen to share a parent directory, they may be better modeled as 3 independent `type: "guide"` packages without a parent `type: "path"` wrapper. In that case, remove the path-level `manifest.json` and `content.json`, and remove the `depends`/`recommends` chain from the step manifests.

3. **Should each step keep its own targeting?** Unlike typical learning path steps (which inherit targeting from the path), each sub-guide here has its own `index.json` rule with a distinct `urlPrefix` and `source: "play.grafana.org"`. The current migration preserves step-level targeting AND aggregates it at the path level via `or`. A reviewer should confirm this is the desired behavior.

4. **Author team:** Used `"interactive-learning"` (standalone guide default) rather than `"Grafana Documentation"` (learning path default) because this content does not originate from the website docs team. Reviewer should confirm.

## Files Created

- `manifest.json` -- path-level manifest (type: "path", 3 milestones)
- `content.json` -- path-level cover page (new, did not exist before)
- `main-page/manifest.json` -- step 1 manifest
- `visualization-page/manifest.json` -- step 2 manifest
- `datasource-page/manifest.json` -- step 3 manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` (path) | directory name | `welcome-to-play` |
| `id` (main-page) | content.json | `welcome-to-play-main` |
| `id` (visualization-page) | content.json | `welcome-to-play-visualization` |
| `id` (datasource-page) | content.json | `welcome-to-play-datasource` |
| `type` (path) | Mode 2 detection | `path` |
| `description` (all) | index.json rule (all 3 share same description) | "Comprehensive walkthrough of Grafana Play features and capabilities." |
| `category` | fallback (no website markdown, no journey.group) | `general` |
| `author.name` | CODEOWNERS (`/welcome-to-play/ @Jayclifford345`) | `Jayclifford345` |
| `author.team` | standalone guide convention | `interactive-learning` |
| `startingLocation` (path) | index.json main-page rule urlPrefix | `/d/to6j8mh/grafana-play-home` |
| `startingLocation` (each step) | respective index.json rule urlPrefix | step-specific dashboard paths |
| `targeting` (path) | aggregated from 3 index.json rules via `or` | see manifest |
| `targeting` (each step) | respective index.json rule verbatim | see step manifests |
| `testEnvironment` | index.json `source: "play.grafana.org"` | `{ "tier": "cloud", "instance": "play.grafana.org" }` |
| `milestones` | ordered by: main-page first (homepage), then visualization, then datasource | 3 IDs |

## Flags for Manual Review

- Category defaulted to `"general"` -- no website markdown or journey.group available
- Author team set to `"interactive-learning"` -- reviewer should confirm this is not a docs-team learning path
- Step ordering is assumed (main -> visualization -> datasource) based on content flow; no `weight` data available
- All three step descriptions are identical (copied from index.json) -- see Duplicate Descriptions below

## Duplicate Descriptions

All three index.json rules share the identical description: "Comprehensive walkthrough of Grafana Play features and capabilities." This was used verbatim for all three step manifests and the path manifest. A reviewer should consider writing distinct descriptions for each step:
- main-page: could focus on the homepage tour and navigation
- visualization-page: could focus on visualization types
- datasource-page: could focus on data source connectivity

## Recommender Rules

No recommender rules found for `welcome-to-play`. This is expected -- the guide targets `play.grafana.org` specifically and is routed via `index.json` rules (type: "interactive"), not via the recommender (type: "learning-journey").

## Data Quality Issues

- All three index.json rules share identical `description` text -- recommend differentiating them upstream
- No website markdown exists for this guide at `/Users/davidallen/hax/website/content/docs/learning-paths/welcome-to-play/`
- No `journeys.yaml` entry exists for this guide
- Step ordering was inferred from content (homepage first, then sub-pages) -- no weight data available
- Each step has its own `pathfinder_data`-less mapping; directory names were used for matching

## Surprises / Notes

- This guide is play.grafana.org-specific (all match rules require `source: "play.grafana.org"`), which is unusual -- most guides target broad URL patterns
- The sub-guides contain mostly markdown-heavy informational content with only a few interactive highlight actions each
- No `-lj` suffix on the directory name despite having nested content.json directories

## TODO

- [ ] TODO(review): Structural decision required -- determine if `type: "path"` with milestones is correct, or if these should be 3 standalone guides (metapackage), or some other structure
- [ ] TODO(review): Step ordering (main -> visualization -> datasource) is assumed, not derived from weight data -- reviewer must confirm or reorder
- [ ] TODO(review): `depends` chain across steps may be incorrect if steps are meant to be consumed independently -- reviewer should remove `depends`/`recommends` if steps are not sequential
- [ ] TODO(review): Author team `"interactive-learning"` vs `"Grafana Documentation"` -- reviewer must confirm
- [ ] TODO(description): All 3 steps share identical description "Comprehensive walkthrough of Grafana Play features and capabilities." -- reviewer should write distinct per-step descriptions
- [ ] TODO(review): Category `"general"` is a fallback -- reviewer should assign a more specific category if one applies
