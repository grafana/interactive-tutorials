# Shared Critical Rules

The canonical critical rules list is maintained in `AGENTS.md` (repo root). Read that file and apply all rules in the **Critical Rules** section. Every rule is blocking.

---

<!-- EXTENSION POINT: Skill-specific rules go here in the file that includes these shared rules -->
