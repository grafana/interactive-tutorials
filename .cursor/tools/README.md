This directory contains **only** scripts which are invoked by Cursor skills.

**Requires Python 3.10+** (uses `X | Y` union type syntax in type hints).

Editing or reusing these scripts is not recommended unless it is in the context of
improving/changing the associated cursor skills.