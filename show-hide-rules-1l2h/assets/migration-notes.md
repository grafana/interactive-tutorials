---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-07-20T20:05:49Z"
status: incomplete
---

# Migration Notes: show-hide-rules-1l2h

## Files Created

- `manifest.json` — standalone guide manifest for "Create a time-based show/hide rule"

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `show-hide-rules-1l2h` |
| `type` | fixed for Mode 1 | `guide` |
| `description` | no source found; summarized from `content.json` title + intro markdown | "Hands-on guide: Learn how to configure a time-based show/hide rule so a dashboard panel appears only when the selected time range is 1 hour or less." |
| `category` | not inside a `*-lj` directory | `general` |
| `author.team` | standalone guide | `interactive-learning` |
| `author.name` | git history (`git log --follow`) — sole non-bot author | `imatwawana` |
| `startingLocation` | no matching `index.json` rule | omitted |
| `targeting` | no matching `index.json` rule | omitted |
| `testEnvironment` | no match expression to derive from | `{ "tier": "cloud" }` (minimum default) |
| `depends`/`recommends`/`suggests`/`provides` | none available | `[]` |

## Flags for Manual Review

- No `index.json` rule matches `show-hide-rules-1l2h` — the guide currently has no contextual targeting/recommendation entry. If this guide should be surfaced contextually (e.g., on the dashboard edit page), add an `index.json` rule and re-derive `targeting`/`startingLocation`.
- `description` was summarized rather than sourced from a rule — reviewer should confirm wording matches desired catalog tone.

## TODO

- [ ] TODO(review): confirm `description` wording is acceptable for the catalog listing.
- [ ] TODO(review): decide whether `show-hide-rules-1l2h` needs an `index.json` targeting rule (currently unrecommended contextually); if added, re-run migration to populate `targeting`/`startingLocation`.
- [ ] TODO(fallback): `validate --package` could not be run — no local `grafana-pathfinder-app` checkout with a built CLI (`dist/cli/cli/index.js`) was found on this machine. Run validation manually once a checkout is available:
  `node {pathfinder-app}/dist/cli/cli/index.js validate --package show-hide-rules-1l2h`
