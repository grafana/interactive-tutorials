---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-17T20:07:03Z"
status: incomplete
---

# Migration Notes: instrumentation-hub-discover-lj

## Files Created

- `manifest.json` — path-level manifest (type: "path", 8 milestones)
- `content.json` — path-level cover page derived from welcome step content
- `welcome/manifest.json` — step 1 manifest
- `why-instrumentation-hub/manifest.json` — step 2 manifest
- `access-instrumentation-hub/manifest.json` — step 3 manifest
- `view-discovered-services/manifest.json` — step 4 manifest
- `select-service/manifest.json` — step 5 manifest
- `enable-instrumentation/manifest.json` — step 6 manifest
- `verify-data-flow/manifest.json` — step 7 manifest
- `end-journey/manifest.json` — step 8 manifest

## Field Derivation Summary

### Path-level manifest

| Field | Source | Value |
|-------|--------|-------|
| `id` | Directory name | `instrumentation-hub-discover-lj` |
| `type` | Convention | `path` |
| `description` | Summarized from welcome step content.json (fallback — no recommender/index.json/website rule) | "Step-by-step guide: Discover Kubernetes services and enable zero-code instrumentation using Instrumentation Hub in Grafana Cloud." |
| `category` | Fallback default (no website markdown) | `general` |
| `author.team` | Convention (learning path) | `Grafana Documentation` |
| `author.name` | Git history (sole author) | `Chris Moyer` |
| `targeting` | Omitted — no recommender or index.json rules found | _(absent)_ |
| `startingLocation` | Omitted — no match expression to derive URL from | _(absent)_ |
| `testEnvironment.tier` | Default (no match expression) | `cloud` |
| `milestones` | Derived from "next milestone" cross-references in step content.json files | 8 steps ordered |
| `recommends` | Empty — no website markdown `journey.links.to` | `[]` |
| `suggests` | Empty — no website markdown `related_journeys` | `[]` |
| `depends` | Empty — no prerequisite data available | `[]` |

### Step manifests (all 8 steps)

| Field | Source | Notes |
|-------|--------|-------|
| `id` | Each step's `content.json` `id` field | Verbatim |
| `type` | Convention | `guide` |
| `description` | Summarized from step content.json title + body content | No index.json, website markdown, or recommender descriptions available |
| `category` | Inherited from path | `general` (fallback) |
| `author.team` | Convention (learning path step) | `Grafana Documentation` |
| `author.name` | Git history (sole author of all steps) | `Chris Moyer` |
| `testEnvironment.tier` | Inherited from path | `cloud` |
| `depends` | Step ordering (step N+1 depends on step N) | Per convention |
| `recommends` | Step ordering (step N recommends step N+1) | Per convention |

### Path-level content.json

| Field | Source | Notes |
|-------|--------|-------|
| `id` | Directory name | `instrumentation-hub-discover-lj` |
| `title` | Welcome step content.json title | "Discover and instrument your first service using Instrumentation Hub" |
| `blocks` | Welcome step content.json (with transformations) | Removed image link, removed Grafana Cloud account prerequisite |

## Content Removed During Migration

- Removed image: `![Instrumentation Hub showing discovered services](https://grafana.com/media/docs/grafana-cloud/instrumentation/ihub-first-page.png)` — grafana.com media URL not suitable for Pathfinder rendering
- Removed prerequisite: `"A Grafana Cloud account. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)."` — redundant for Pathfinder users who are already in Grafana

## Flags for Manual Review

- `category` used fallback value `"general"` — no website markdown `journey.group` available. Reviewer should determine the correct category (likely `"observability"` or `"data-availability"`).
- `targeting` omitted — no recommender rules and no index.json rules found for this learning path. If contextual recommendations are needed, targeting rules must be added to the recommender repo.
- `startingLocation` omitted — no match expression to derive a URL from. Reviewer may want to set this to the Instrumentation Hub URL path (e.g., `/a/grafana-collector-app/instrumentation-hub`).
- `milestones` ordering derived from content cross-references (not website `weight` values) — reviewer should verify ordering is correct.
- All step `description` fields were summarized from content.json titles/body — no authoritative catalog descriptions existed. Reviewer should verify these are appropriate.
- `recommends`, `suggests`, `depends` on the path manifest are all empty — no website markdown available to derive relationships.

## Recommender Rules

No recommender rules found for this path. Searched all files in `grafana-recommender/internal/configs/state_recommendations/` for entries with `"type": "learning-journey"` whose URL slug matches `instrumentation-hub-discover`. No matches. This learning path has no contextual recommendation targeting and will only be reachable via direct navigation or learning path listings.

## Data Quality Issues

- No website markdown exists at `website/content/docs/learning-paths/instrumentation-hub-discover/` — this path appears to be new content not yet published on the website.
- No CODEOWNERS entry exists for `/instrumentation-hub-discover-lj/` — author attribution came from git history only.
- No `journeys.yaml` entry exists for this path — no cross-validation possible.
- Step ordering was derived from "next milestone" prose references in each step's content.json, not from website `weight` values. All 8 steps used content-based ordering (no `pathfinder_data` or weight values available).

## TODO

- [ ] TODO(review): `category` is `"general"` (fallback) — reviewer must determine correct category value
- [ ] TODO(review): `targeting` is absent — reviewer must decide whether recommender rules should be created for this path
- [ ] TODO(review): `startingLocation` is absent — reviewer should set to the Instrumentation Hub URL path if desired
- [ ] TODO(review): `milestones` ordering is derived from content cross-references — reviewer must verify ordering
- [ ] TODO(review): step `description` values are summarized from content — reviewer should verify catalog descriptions
- [ ] TODO(review): `recommends`, `suggests`, `depends` on path manifest are empty — reviewer should add relationships when website markdown is published
- [ ] TODO(review): CLI validation could not run — Pathfinder CLI not built (`grafana-pathfinder-app/dist/` does not exist). Reviewer must build the CLI and run `node dist/cli/cli/index.js validate --package <dir>` for each package.
