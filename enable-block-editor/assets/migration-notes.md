---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: enable-block-editor

## Files Created

- `manifest.json` — standalone guide manifest derived from content.json id/title and index.json rule

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `"enable-block-editor"` |
| `type` | constant | `"guide"` |
| `description` | `index.json` rule (verbatim) | `"Hands-on guide: Learn how to enable the Block Editor for first-time authors."` |
| `category` | default (not inside *-lj) | `"general"` |
| `author.team` | constant (standalone guide) | `"interactive-learning"` |
| `author.name` | git log --follow on content.json | `"Jayclifford345, Tom Glenn"` |
| `startingLocation` | first URL-bearing leaf of index.json match (depth-first) | `"/d/ja6hlhh/welcome-to-learn-grafana"` |
| `targeting.match` | `index.json` rule (verbatim copy) | `or` with two `urlPrefix` leaves |
| `testEnvironment.tier` | tier inference: match has no `source` or `targetPlatform: cloud` → local | `"local"` |
| `depends` | no data available | `[]` |
| `recommends` | no data available | `[]` |
| `suggests` | no data available | `[]` |
| `provides` | no data available | `[]` |

## Validator Result

**CLI unavailable (blocker).** The Pathfinder CLI at `dist/cli/cli/index.js` was not found in either the `interactive-tutorials` repo or the `grafana-pathfinder-app` checkout at `/Users/davidallen/hax/grafana-pathfinder-app/dist/cli/`. The `dist/` directory exists but does not contain the CLI bundle — it appears the CLI has not been built. To complete Phase 1 validation, run `npm run build` (or equivalent) in the `grafana-pathfinder-app` checkout and then re-run:

```bash
node /Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js validate --package /Users/davidallen/hax/interactive-tutorials/enable-block-editor
```

## Surprises / Notes

- The `enable-block-editor` guide targets the Pathfinder plugin configuration page (`/plugins/grafana-pathfinder-app`) and a specific "Welcome to Learn Grafana" dashboard. This is an internal/authoring-tooling guide rather than a product feature guide, which is unusual among the guides in `index.json`.
- `testEnvironment.tier` is `"local"` because the match expression uses only `urlPrefix` predicates (no `source` and no `targetPlatform: "cloud"`). This is the correct inference per the tier rules, but may warrant review — it is plausible this guide should be tested against a specific Grafana Cloud instance where the Pathfinder plugin is installed.

## TODO

- [ ] Run `node dist/cli/cli/index.js validate --package enable-block-editor` once the CLI is built and confirm the manifest passes validation.
- [ ] Review `testEnvironment.tier: "local"` — confirm whether this guide should target `"cloud"` instead (if it requires the Pathfinder plugin to be installed on a Grafana Cloud instance).
- [ ] Review `author.name` values (`Jayclifford345, Tom Glenn`) and confirm these are the correct attributions.
