---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-13T00:00:00Z"
status: complete
---

# Migration Notes: github-visualize-lj

## Files Created

- `manifest.json` — path-level manifest (type: "path", 5 milestones, targeting from recommender)
- `content.json` — path-level cover page derived from website `_index.md`
- `business-value/manifest.json` — step 1 manifest
- `build-issues-panel/manifest.json` — step 2 manifest
- `build-pr-panel/manifest.json` — step 3 manifest
- `build-repository-panel/manifest.json` — step 4 manifest
- `end-journey/manifest.json` — step 5 manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` (path) | directory name | `github-visualize-lj` |
| `type` (path) | — | `"path"` |
| `description` (path) | recommender `dashboards-cloud.json` | "A learning journey that shows you how to visualize GitHub repository data in Grafana Cloud." |
| `category` | `_index.md` `journey.group` | `"query-and-visualize"` |
| `author.name` | git history (no CODEOWNERS entry for this LJ) | `bonnywelsford-source` |
| `author.team` | content type (learning path) | `"Grafana Documentation"` |
| `startingLocation` | recommender rule `urlPrefix` | `/dashboards` |
| `targeting.match` | recommender `dashboards-cloud.json` | `{ "urlPrefix": "/dashboards" }` |
| `testEnvironment` | recommender match has no `source` or `targetPlatform` → cloud default | `{ "tier": "cloud" }` |
| `milestones` | website step `weight` ordering | see order below |
| `depends` (path) | `_index.md` "Before you begin" explicitly requires prior completion of github-data-source LJ | `["github-data-source-lj"]` |
| Step descriptions | website step `index.md` `description` field | verbatim (already catalog-style) |
| Step `depends`/`recommends` | sequential step ordering | derived from milestone order |

## Milestone Order (from website weight field)

1. `github-visualize-business-value` (weight 350)
2. `github-visualize-build-issues-panel` (weight 400)
3. `github-visualize-build-pr-panel` (weight 500)
4. `github-visualize-build-repository-panel` (weight 600)
5. `github-visualize-end-journey` (weight 700)

No step had `pathfinder_data` frontmatter — all were mapped by directory name matching.

## Content Removed During Migration

- Removed image: `{{< figure alt="Example GitHub dashboard..." src="/media/docs/grafana/data-sources/github/github-dashboard-1.png" >}}` — website-relative image path
- Removed prerequisite: "A Grafana Cloud account. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)." — redundant for Pathfinder users
- Stripped Hugo shortcodes (`{{< docs/box >}}`, `{{< docs/icon-heading >}}`, `{{< /docs/box >}}`) — preserved inner content

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| `dashboards-cloud.json` | `https://grafana.com/docs/learning-journeys/github-visualize/` | `{ "urlPrefix": "/dashboards" }` |

## Dangling References

- `depends: ["github-data-source-lj"]` on path manifest — `github-data-source-lj` exists on `main` but is not present on this PR branch. The Pathfinder CLI will flag this; it is expected and correct.

## Surprises / Notes

- No CODEOWNERS entry exists specifically for `github-visualize-lj/`. The `bonnywelsford-source` author attribution comes from git history on this PR branch.
- The Pathfinder CLI (`node dist/cli/cli/index.js validate --package`) was not run — CLI is not available in this environment. Validation is deferred to CI.
