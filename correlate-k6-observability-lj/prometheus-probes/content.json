{
  "schemaVersion": "1.0.0",
  "id": "correlate-k6-observability-prometheus-probes",
  "title": "Compare probe latency in Prometheus",
  "blocks": [
    {
      "type": "markdown",
      "content": "Grafana **Explore** is where you query metrics time series with PromQL. In Grafana Cloud those series usually come from a Prometheus-compatible data source (often Grafana-hosted metrics storage, not a Prometheus server you run yourself). Synthetic checks and many application metrics show up there, so you can graph probe latency for the same wall-clock span as your k6 run. This milestone starts with metrics because they give a quick, regional view of health before you dig into logs and traces. Refer to [Explore](/docs/grafana/latest/explore/) if you need a refresher on the page.\n\nYou finished the spike and k6 review milestones in Grafana Cloud k6 with a UTC window and summary numbers. This milestone focuses on **synthetic probe** latency in your Prometheus-compatible data source for that same window (separate from the `grafanacloud-k6` generator metrics you aligned earlier).\n\nk6 and synthetic monitoring solve different problems day to day. Here you only borrow probe time series as a second camera on the spike: probes keep their normal schedule, and Prometheus stores their samples during your test window. That lets you compare what outside regions saw against what the load generator reported, without treating runtime checks as a substitute for a performance test.\n\nSynthetic probes measure the endpoint from multiple regions. They often diverge from k6\u2019s numbers because path, DNS, TLS, geography, and concurrency differ. Your job is to see whether user-visible latency spiked in the same window as the test.\n\nTo analyze probe metrics, complete the following steps.\n\n1. In the main Grafana navigation on the left, open **Explore**. If the navigation is collapsed, open it with the menu control, then choose **Explore**. Select your Prometheus-compatible data source (demo stacks sometimes use a name like `grafanacloud-demoinfra-prom`; yours may differ).\n1. Set the time range to cover your test window from the previous milestone (include warm-up and cool-down).\n1. Run this query and switch to a **Time series** visualization:\n\n```promql\nprobe_http_duration_seconds\n```\n\nIf the metric is absent, ask your administrator which metric exposes synthetic HTTP latency for the quickpizza checks and substitute it while keeping the same questions.\n\nFrom the chart, answer:\n\n- Did any probe region show a latency spike during the test window?\n- Which region peaked highest?\n- Which region was slowest to recover toward baseline?\n- Roughly how does the worst probe peak compare to k6\u2019s p95 (for example, higher by an order of magnitude)?\n\nRun:\n\n```promql\nprobe_duration_seconds\n```\n\n(or the closest probe-phase metric your environment exposes).\n\nAnswer:\n\n- Did latency return to baseline by the end of your selected range?\n- If you were a user in the slowest-recovering region, how many minutes of degraded experience would you infer from the chart?\n\nReflection: Why might probes report higher latency than k6 even when both target the same URL?\n\nCheck your work: you can move on when your **Explore** time range matches the k6 window, both probe queries return data (or you documented an equivalent metric name), you can name which region peaked and whether recovery lagged, and you wrote a short answer to the reflection prompt. If panels are empty, widen the range a few minutes, confirm the data source, then open the troubleshooting topics for this milestone.\n\nIn the next milestone you validate whether errors accompanied the latency change."
    }
  ]
}
