---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: rca-demo

## Files Created

- `manifest.json` — standalone guide manifest generated from content.json `id`/`title`, git history, and derivation rules; no index.json rule was found for this guide.

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `rca-demo` |
| `type` | Convention | `"guide"` |
| `description` | Summarized from `content.json` title and intro prose (no index.json rule) | `"Hands-on demo: Walk through an end-to-end Root Cause Analysis using Grafana's RCA workbench, from alert to investigation across services and databases, through blast radius and business impact."` |
| `category` | Convention (not inside `*-lj`) | `"general"` |
| `author.team` | Convention (standalone guide) | `"interactive-learning"` |
| `author.name` | `git log --follow rca-demo/content.json` | `"Tom Glenn"` |
| `targeting` | No index.json rule found | omitted |
| `startingLocation` | No match expression to extract URL from | omitted |
| `testEnvironment.tier` | No match expression → minimum default | `"cloud"` |
| `depends`, `recommends`, `suggests`, `provides` | No data available | `[]` |

## No Targeting — Expected

No `index.json` rule exists for `rca-demo`. This is not a migration error — targeting was simply not available to migrate. `targeting` and `startingLocation` are correctly omitted from the manifest.

## Flags for Manual Review

- **`description`** was synthesized from `content.json` — it was not taken from an `index.json` rule (none exists). Review and adjust the description for catalog-appropriateness.
- **`testEnvironment.tier`** defaults to `"cloud"` because no match expression exists. The guide itself navigates to `/a/grafana-asserts-app/` which is a cloud/on-prem app — `"cloud"` is a reasonable default, but verify against the intended deployment environment.

## Validator Result

Command: `node /Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js validate --package /Users/davidallen/hax/interactive-tutorials/rca-demo`

Result: **PASS** (exit 0)

Warnings (all expected):
- `manifest.json: "targeting" not specified` — expected, no index.json rule found
- `manifest.json: "startingLocation" not specified, defaulting to "/"` — expected, omitted intentionally per convention

## Surprises / Notes

- The `dist/cli/cli/index.js` was not present in the `interactive-tutorials` repo root (the path `interactive-tutorials/dist/cli/cli/index.js` does not exist). The validator was found at `/Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js`.
- `rca-demo` is a demo-only guide (navigates to custom RCA demo dashboards and the `grafana-asserts-app`). It appears to be an internally-authored demo rather than a public-facing tutorial, which may explain the absence of an `index.json` rule.

## TODO

- [ ] Decide whether `rca-demo` should have contextual targeting; if yes, add an `index.json` rule and re-run migration.
- [ ] Review synthesized `description` for catalog appropriateness.
- [ ] Confirm `testEnvironment.tier: "cloud"` is correct for the intended deployment environment.
