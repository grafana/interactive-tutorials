---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: adaptive-logs-recommendations

## Files Created

- `manifest.json` — standalone guide manifest, derived from index.json rule match and content.json id

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `adaptive-logs-recommendations` |
| `type` | fixed | `guide` |
| `description` | `index.json` rule | `"Hands-on guide: Reduce log volume safely with Adaptive Logs."` |
| `category` | default (not inside a `*-lj` directory) | `general` |
| `author.team` | convention (standalone guide) | `interactive-learning` |
| `author.name` | git log (`--follow`) | `brendamuir, Jayclifford345` (bots filtered out) |
| `startingLocation` | — | omitted (no confirmed index.json match) |
| `targeting` | — | omitted (no confirmed index.json match) |
| `testEnvironment.tier` | default (no match expression) | `cloud` |

## Flags for Manual Review

- **No targeting:** A candidate rule (`reduce-log-volume-adaptive-logs`) was found in `index.json` with matching title and subject matter, but its URL segment does not formally match the directory name or id (`adaptive-logs-recommendations`). Targeting was omitted rather than using an unconfirmed match. To add targeting, either update the `index.json` rule URL to reference `adaptive-logs-recommendations`, or add a new rule with the correct URL segment.

## Validator Output

```
✅ adaptive-logs-recommendations (adaptive-logs-recommendations)
  ℹ️  INFO: manifest.json: "repository" not specified, defaulting to "interactive-tutorials"
  ℹ️  INFO: manifest.json: "language" not specified, defaulting to "en"
  ℹ️  INFO: manifest.json: "schemaVersion" not specified, defaulting to "1.1.0"
  ℹ️  INFO: manifest.json: "conflicts" not specified, defaulting to []
  ℹ️  INFO: manifest.json: "replaces" not specified, defaulting to []
```

All INFOs are expected schema defaults. Validation passed with no errors or warnings.

## Surprises / Notes

- The `Cursor Agent` commit author was filtered from `author.name` as it is automation.
- The `author.name` field contains two GitHub handles: `brendamuir` and `Jayclifford345`, identified from `git log --follow` on `content.json`.
- The validator CLI was not present at `dist/cli/cli/index.js` in the interactive-tutorials repo root. It was found and run successfully from `/Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js`.

## TODO

- [ ] Add targeting: update the `index.json` rule URL from `reduce-log-volume-adaptive-logs` to `adaptive-logs-recommendations` (or add a new rule), then re-run migration to populate `targeting` and `startingLocation`.
