menuTitle: Export JSON
title: Export dashboard JSON
description: Call build() to validate the dashboard, serialize it to JSON, and write
  it to a file.
weight: 500
step: 6
layout: single-journey
cta:
  type: continue
keywords:
- Foundation SDK
- JSON
- build
- export
