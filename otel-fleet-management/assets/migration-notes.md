---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-06T00:00:00Z"
---

# Migration Notes: otel-fleet-management

## Files Created

- `manifest.json` — standalone guide manifest (Mode 1)

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| id | content.json | `otel-fleet-management` |
| type | convention | `guide` |
| description | kiosk/GrafanaCON2026/rules.json | Catalog-style description from kiosk rule |
| category | default (no parent LJ) | `general` |
| author.team | convention (standalone guide) | `interactive-learning` |
| author.name | git log | `Jayclifford345` |
| testEnvironment | default (no index.json rule) | `{ "tier": "cloud" }` |
| targeting | n/a | Omitted — no matching index.json rule |
| startingLocation | n/a | Omitted — no match expression to derive from |

## Flags for Manual Review

- No `index.json` rule exists for this guide. The `targeting` and `startingLocation` fields are omitted. If this guide should appear as a contextual recommendation, add a rule to `index.json` and update the manifest accordingly.
- The `description` was sourced from `kiosk/GrafanaCON2026/rules.json` rather than `index.json` (which has no entry for this guide). Verify the description is appropriate for a catalog listing.

## Validation Results

```
$ node dist/cli/cli/index.js validate --package otel-fleet-management

✅ otel-fleet-management (otel-fleet-management)
  ℹ️  INFO: defaults applied for repository, language, schemaVersion, conflicts, replaces
  ⚠️  WARN: "targeting" not specified
  ⚠️  WARN: "startingLocation" not specified, defaulting to "/"
```

Warnings are expected — targeting and startingLocation are intentionally omitted because no index.json rule exists.

## TODO

- [ ] Add an `index.json` rule for `otel-fleet-management` if contextual recommendation is desired
- [ ] Populate `startingLocation` and `targeting` once the index.json rule is created
