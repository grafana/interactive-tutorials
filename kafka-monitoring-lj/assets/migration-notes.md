---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-07T00:00:00Z"
status: complete
---

# Migration Notes: kafka-monitoring-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 11 milestones, targeting from recommender)
- `content.json` — path-level cover page derived from website `_index.md`
- `value-kafka-monitoring/manifest.json` — step 1 manifest
- `configure-jmx-exporter/manifest.json` — step 2 manifest
- `advantages-grafana-alloy/manifest.json` — step 3 manifest
- `install-grafana-alloy/manifest.json` — step 4 manifest
- `configure-alloy-kafka-metrics/manifest.json` — step 5 manifest
- `install-dashboards/manifest.json` — step 6 manifest
- `verify-kafka-metrics/manifest.json` — step 7 manifest
- `understand-dashboards/manifest.json` — step 8 manifest
- `understand-alerts/manifest.json` — step 9 manifest
- `explore-kafka-metrics/manifest.json` — step 10 manifest
- `end-journey/manifest.json` — step 11 manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` (path) | directory name | `kafka-monitoring-lj` |
| `type` (path) | convention | `path` |
| `description` (path) | recommender rule (connections-cloud.json) | "Step-by-step guide: Monitor Kafka with Grafana Cloud." |
| `category` | website `_index.md` `journey.group` | `data-availability` |
| `author.name` | CODEOWNERS (`/kafka-monitoring-lj/ @tacole02`) | `tacole02` |
| `author.team` | convention (learning path) | `Grafana Documentation` |
| `startingLocation` | recommender match expression, first URL-bearing leaf | `/connections/add-new-connection/kafka` |
| `targeting.match` | recommender (connections-cloud.json) | `{"and": [{"or": [{"urlPrefixIn": ["/connections/add-new-connection/kafka", "/connections/infrastructure"]}, {"urlRegex": "/connections/add-new-connection/?$"}]}, {"targetPlatform": "cloud"}]}` |
| `testEnvironment` | match contains `targetPlatform: cloud` | `{"tier": "cloud"}` |
| `milestones` | website step `weight` ordering | 11 steps ordered by weight |
| `suggests` (path) | end-journey `related_journeys` | `drilldown-metrics-lj`, `infrastructure-alerting-lj`, `drilldown-logs-lj` |
| step descriptions | website step `index.md` `description` frontmatter | one-line descriptions per step |
| step `depends`/`recommends` | sequential ordering by weight | chain: each step depends on previous, recommends next |

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| connections-cloud.json | `https://grafana.com/docs/learning-paths/kafka-monitoring/` | `{"and": [{"or": [{"urlPrefixIn": ["/connections/add-new-connection/kafka", "/connections/infrastructure"]}, {"urlRegex": "/connections/add-new-connection/?$"}]}, {"targetPlatform": "cloud"}]}` |

## Data Quality Issues

- No entry for `kafka-monitoring` found in `journeys.yaml`. This path appears to be a newer addition not yet registered in the journey graph.
- All 11 steps had `pathfinder_data` frontmatter present in website markdown; no directory name fallback was needed.
- The `_index.md` frontmatter has no `journey.links.to` field, so the path-level `recommends` is empty. The `related_journeys` from the `end-journey` step were mapped to path-level `suggests`.

## Dangling References

- `drilldown-metrics-lj` in path `suggests` — directory exists in repo
- `infrastructure-alerting-lj` in path `suggests` — directory exists in repo
- `drilldown-logs-lj` in path `suggests` — directory exists in repo

(All referenced directories exist; no dangling references.)

## Validation Results

- Path package (`kafka-monitoring-lj`): PASS
- All 11 step packages: PASS
- Step-level warnings for missing `targeting` and `startingLocation` are expected (targeting lives at path level)
