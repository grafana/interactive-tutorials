---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: understanding-the-four-golden-signals-of-observability

## Files Created

- `manifest.json` — standalone guide manifest generated from index.json rule and content.json

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `understanding-the-four-golden-signals-of-observability` |
| `type` | convention | `"guide"` (standalone, not inside a `*-lj` directory) |
| `description` | `index.json` rule | `"Learn about the Four Golden Signals — Latency, Traffic, Errors, and Saturation — with conceptual explanations and interactive dashboard examples."` |
| `category` | convention | `"general"` (not inside a `*-lj` directory; no website markdown path applies) |
| `author.team` | convention | `"interactive-learning"` (standalone guide) |
| `author.name` | git log | `"Jayclifford345, Knappek"` (human contributors from git history; no bots detected) |
| `startingLocation` | `index.json` match — first URL-bearing leaf | `"/a/grafana-setupguide-app/home"` (first entry of `urlPrefixIn` in the `and` combinator) |
| `targeting.match` | `index.json` rule | copied verbatim |
| `testEnvironment.tier` | `index.json` match tier inference | `"cloud"` — match contains `"targetPlatform": "cloud"` but no `source` field |
| `depends` | — | `[]` (empty; no known prerequisites) |
| `recommends` | — | `[]` (empty; no known follow-ups) |
| `suggests` | — | `[]` (empty) |
| `provides` | — | `[]` (empty; semantic outcomes not yet defined) |

## Validator Result

```
✅ understanding-the-four-golden-signals-of-observability (understanding-the-four-golden-signals-of-observability)
  ℹ️  INFO: manifest.json: "repository" not specified, defaulting to "interactive-tutorials"
  ℹ️  INFO: manifest.json: "language" not specified, defaulting to "en"
  ℹ️  INFO: manifest.json: "schemaVersion" not specified, defaulting to "1.1.0"
  ℹ️  INFO: manifest.json: "conflicts" not specified, defaulting to []
  ℹ️  INFO: manifest.json: "replaces" not specified, defaulting to []
```

All INFO messages are expected schema defaults — no warnings or errors.

## Surprises / Notes

- The index.json rule was found without issue; all fields derived from first-priority sources (no fallbacks needed).
- The `testEnvironment` tier is `"cloud"` rather than `"local"` because the match expression includes `"targetPlatform": "cloud"`. No `source` field is present, so no `instance` was set.
- The guide uses `grafana-demodashboards-app` (QuickPizza) at runtime, which requires a cloud instance with the demo app installed. The `cloud` tier is semantically correct.
- Two distinct git contributors identified: `Jayclifford345` (jay.clifford@grafana.com) and `Knappek`/`Andy Knapp` (andy.knapp.ak@gmail.com), both using GitHub handles in the `author.name` field.

## TODO

- [ ] Populate `provides` when semantic capability tokens are defined for this guide type.
- [ ] Verify `startingLocation` (`/a/grafana-setupguide-app/home`) is the intended launch point — the match also covers `/explore` and `/drilldown`.
