---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: k8s-cpu

## Files Created

- `manifest.json` — standalone guide manifest derived from index.json rule and content.json

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `"k8s-cpu"` |
| `type` | convention | `"guide"` |
| `description` | `index.json` rule (verbatim) | `"Hands-on guide: Explore CPU usage in Kubernetes Monitoring, from namespaces to containers."` |
| `category` | default (not inside *-lj) | `"general"` |
| `author.team` | convention (standalone guide) | `"interactive-learning"` |
| `author.name` | git log (`--follow`) | `"BeverlyJaneJ, David Allen, Tom Glenn"` |
| `startingLocation` | first URL-bearing leaf from match | `"/a/grafana-k8s-app"` |
| `targeting.match` | `index.json` rule (verbatim) | `{ "and": [{ "urlPrefix": "/a/grafana-k8s-app" }, { "source": "play.grafana.org" }] }` |
| `testEnvironment.tier` | `source: "play.grafana.org"` in match (concrete hostname → cloud) | `"cloud"` |
| `testEnvironment.instance` | `source` value in match | `"play.grafana.org"` |
| `depends` | none | `[]` |
| `recommends` | none | `[]` |
| `suggests` | none | `[]` |
| `provides` | none | `[]` |

## Validator Result

**BLOCKER: CLI unavailable.** The Pathfinder CLI was not found at either:
- `/Users/davidallen/hax/interactive-tutorials/dist/cli/cli/index.js`
- `/Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js`

The `grafana-pathfinder-app` repo is checked out at `/Users/davidallen/hax/grafana-pathfinder-app/` but the CLI has not been built (no `dist/cli/` directory). To validate, run `yarn build` or the equivalent CLI build step in that repo, then re-run:

```bash
node /Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js validate --package /Users/davidallen/hax/interactive-tutorials/k8s-cpu
```

## Flags for Manual Review

- `author.name`: Derived from git log. Three authors found: Beverly Buchanan (GitHub handle: BeverlyJaneJ), David Allen, Tom Glenn. Confirm this attribution is correct or adjust as needed.
- `category`: Defaulted to `"general"` — this guide is not inside a `*-lj` directory so no `journey.group` source exists. Override if a more specific category is appropriate.

## Surprises / Notes

- The `k8s-cpu` and `k8s-mem` guides share identical `match` expressions in `index.json` (both match `/a/grafana-k8s-app` + `source: play.grafana.org`). This is intentional — both guides cover different aspects of the same product (CPU vs memory usage in Kubernetes Monitoring) and are designed to surface together in the same context.

## TODO

- [ ] Build the Pathfinder CLI and run `validate --package k8s-cpu` to confirm the manifest passes schema validation.
- [ ] Confirm `author.name` attribution is accurate.
- [ ] Consider whether `category` should be something more specific than `"general"` (e.g., `"kubernetes"` or `"infrastructure"`).
- [ ] Confirm `author.name` attribution (BeverlyJaneJ, David Allen, Tom Glenn) is accurate.
