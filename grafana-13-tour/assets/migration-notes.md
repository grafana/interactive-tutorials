# Migration Notes: grafana-13-tour

Scaffold created for a new Grafana 13 feature tour. Update `manifest.json` targeting (`startingLocation`, `targeting.match`) when you know the exact URLs and environments (Grafana Play vs. other instances).

Validate with your packaging workflow when ready (for example `--package` if your team uses the migrate-guide tooling).
