# Migration Notes: grafana-13-tour-play

Scaffold for the Grafana 13 feature tour on **Grafana Play** (`play.grafana.org`). Update `manifest.json` targeting (`startingLocation`, `targeting.match`) when you know the exact URLs and environments.

Validate with your packaging workflow when ready (for example `--package` if your team uses the migrate-guide tooling).
