---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-14T00:00:00Z"
status: incomplete
---

# Migration Notes: postgresql-db-olly-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 7 milestones, targeting from recommender)
- `content.json` — path-level cover page (content from former `welcome` step, with Grafana Cloud account prerequisite removed)
- `business-value/manifest.json` — step 1 manifest
- `check-requirements/manifest.json` — step 2 manifest
- `prepare-postgresql/manifest.json` — step 3 manifest
- `configure-alloy/manifest.json` — step 4 manifest
- `verify-telemetry/manifest.json` — step 5 manifest
- `explore-queries/manifest.json` — step 6 manifest
- `end-journey/manifest.json` — step 7 manifest

The `welcome/` directory was removed entirely — its content now lives in the path-level `content.json`.

## Field Derivation Summary

### Path-level manifest

| Field | Source | Value |
|-------|--------|-------|
| `id` | Directory name | `postgresql-db-olly-lj` |
| `type` | Convention | `path` |
| `description` | Recommender rule (`observability-cloud.json`) | "Step-by-step guide: Set up Grafana Cloud Database Observability for PostgreSQL to monitor query performance, explain plans, and wait events." |
| `category` | Website `_index.md` `journey.group` | `data-availability` |
| `author.team` | Learning path convention | `Grafana Documentation` |
| `author.name` | Git history (Chris Moyer) cross-referenced with CODEOWNERS patterns for similar PostgreSQL paths | `chri2547` |
| `startingLocation` | First URL from recommender match expression | `/a/grafana-dbo11y-app` |
| `targeting.match` | Recommender rule (`observability-cloud.json`) | `and: [urlPrefixIn: ["/a/grafana-dbo11y-app", "/a/grafana-dbo11y-app/configuration"], targetPlatform: "cloud"]` |
| `testEnvironment.tier` | Match contains `targetPlatform: "cloud"` | `cloud` |
| `milestones` | Website markdown step numbers (steps 2-8; welcome merged into path-level content.json) | 7 steps in order |
| `recommends` | `_index.md` has no `journey.links.to` | `[]` |
| `suggests` | `end-journey` `related_journeys` | `["drilldown-metrics-lj", "drilldown-logs-lj", "postgresql-integration-lj"]` |

### Step manifests

| Step | Description Source |
|------|-------------------|
| `business-value` | Website `business-value/index.md` description |
| `check-requirements` | Website `check-requirements/index.md` description |
| `prepare-postgresql` | Website `prepare-postgresql/index.md` description |
| `configure-alloy` | Website `configure-alloy/index.md` description |
| `verify-telemetry` | Website `verify-telemetry/index.md` description |
| `explore-queries` | Website `explore-queries/index.md` description |
| `end-journey` | Website `end-journey/index.md` description |

### Path-level content.json

Content was copied from the `welcome` step's `content.json` into the path-level `content.json` (with id `postgresql-db-olly-lj`). The "Grafana Cloud account" prerequisite was removed per Pathfinder conventions (users are already in Grafana). The `welcome/` directory was then removed entirely.

## Content Removed During Migration

- Removed prerequisite: `"A Grafana Cloud account with Editor or Admin role. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)."` — redundant for Pathfinder users who are already in Grafana Cloud.

## Flags for Manual Review

- **`welcome` directory removed** — its content was merged into the path-level `content.json`. The `business-value` step is now the first milestone with no `depends`.
- **`author.name`** — CODEOWNERS has no specific `/postgresql-db-olly-lj/` entry. Used `chri2547` from git history (Chris Moyer) cross-referenced with CODEOWNERS entries for `/postgresql-integration-lj/`. Confirm this is correct.
- **`validate --package` was not run** — the Pathfinder CLI is not built (`node_modules` and `dist/` are missing in the `grafana-pathfinder-app` checkout). Build the CLI and run validation manually.

## Dangling References

No dangling references detected. All `suggests` targets exist as directories in the interactive-tutorials repo:
- `drilldown-metrics-lj` — exists
- `drilldown-logs-lj` — exists
- `postgresql-integration-lj` — exists

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| `observability-cloud.json` | `https://grafana.com/docs/learning-paths/postgresql-db-olly/` | `and: [urlPrefixIn: ["/a/grafana-dbo11y-app", "/a/grafana-dbo11y-app/configuration"], targetPlatform: "cloud"]` |

Only one recommender rule was found for this learning path. Note: the MySQL DB Observability path shares the same match expression — both paths are recommended on the same Database Observability pages.

## Data Quality Issues

- **No `journeys.yaml` entry** — the `postgresql-db-olly` learning path has no entry in `journeys.yaml`. The nearest entry is `monitor-postgreSQL` (category: data-availability), which maps to a different learning path (`postgresql-integration-lj`). The website team should add a `journeys.yaml` entry when the learning path is published.
- **Website markdown on unmerged branch** — the website markdown exists only on the `docs/db-olly-course` branch. All website-derived fields were read from this branch.
- **Shared recommender targeting** — both `mysql-db-olly-lj` and `postgresql-db-olly-lj` match the same recommender rule URLs (`/a/grafana-dbo11y-app`). Both paths will be recommended on Database Observability pages, which is expected behavior.
- **Side journeys** — `configure-alloy`, `prepare-postgresql`, and `explore-queries` have `side_journeys` in their website markdown, but all links point to Grafana docs pages (not learning path URLs), so no `suggests` were derived at the step level.

## TODO

- [ ] TODO(review): Build the Pathfinder CLI and run `validate --package` on `postgresql-db-olly-lj/` and each step directory
- [ ] TODO(review): Confirm `author.name: chri2547` is the correct attribution for this learning path
- [ ] TODO(review): Add a `journeys.yaml` entry for `postgresql-db-olly` when the website learning path is published
