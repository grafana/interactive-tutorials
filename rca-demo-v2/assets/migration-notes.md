---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: rca-demo-v2

## Files Created

- `manifest.json` — standalone guide manifest generated from content.json id/title and git history; no index.json rule existed so targeting and startingLocation are omitted.

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `rca-demo-v2` |
| `type` | Fixed | `"guide"` |
| `description` | Summarized from content.json title + body (no index.json rule) | `"Hands-on demo: Walk through an end-to-end root cause analysis using Grafana's RCA Workbench, from alert to investigation, blast radius, and business impact."` |
| `category` | Default (not inside a `*-lj` directory) | `"general"` |
| `author.name` | `git log --follow` (filtered bots) | `"Tom Glenn"` |
| `author.team` | Standalone guide convention | `"interactive-learning"` |
| `targeting` | OMITTED — no matching index.json rule | — |
| `startingLocation` | OMITTED — no match expression to derive URL from | — |
| `testEnvironment.tier` | Default (no match expression) | `"cloud"` |
| `depends`, `recommends`, `suggests`, `provides` | No data available | `[]` |

## No Targeting — Expected

No `index.json` rule exists for `rca-demo-v2`. This is not a migration error — targeting was simply not available to migrate. `targeting` and `startingLocation` are correctly omitted from the manifest.

## Flags for Manual Review

- **`category`**: Defaulted to `"general"`. If this guide belongs to a specific category (e.g., `"observability"`, `"incident-response"`), update manually.
- **`description`**: Derived by summarizing the content.json body (no index.json rule existed). Review for catalog-style accuracy.

## Validator Result

```
✅ rca-demo-v2 (rca-demo-v2)
  ℹ️  INFO: manifest.json: "repository" not specified, defaulting to "interactive-tutorials"
  ℹ️  INFO: manifest.json: "language" not specified, defaulting to "en"
  ℹ️  INFO: manifest.json: "schemaVersion" not specified, defaulting to "1.1.0"
  ℹ️  INFO: manifest.json: "conflicts" not specified, defaulting to []
  ℹ️  INFO: manifest.json: "replaces" not specified, defaulting to []
  ⚠️  WARN: manifest.json: "targeting" not specified
  ⚠️  WARN: manifest.json: "startingLocation" not specified, defaulting to "/"
```

Warnings for `targeting` and `startingLocation` are expected when no index.json rule exists. The package is otherwise valid.

## TODO

- [ ] Determine whether this guide should have a `targeting` rule in `index.json` (e.g., triggered from `/a/grafana-asserts-app` or similar RCA Workbench URL).
- [ ] Confirm or update `startingLocation` once targeting is established.
- [ ] Review `category` — `"general"` is a fallback; consider a more specific value if applicable.
- [ ] Review `description` for final catalog wording.
