---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-08T00:00:00Z"
status: complete
---

# Migration Notes: visualization-logs

**Special case:** This directory has a learning-path structure (7 nested directories each with content.json) but does NOT end in `-lj`. Mode 2 (learning path) was triggered via the "directory contains nested directories with content.json" condition. The path-level `id` is `visualization-logs` (not `visualization-logs-lj`).

## Files Created

- `manifest.json` -- path-level manifest (type: path, 7 milestones, targeting from recommender)
- `content.json` -- path-level cover page derived from website `_index.md`
- `business-value-visualizations/manifest.json` -- step 1 manifest
- `plan-visualization/manifest.json` -- step 2 manifest
- `add-visualization/manifest.json` -- step 3 manifest
- `write-query/manifest.json` -- step 4 manifest
- `time-range-refresh/manifest.json` -- step 5 manifest
- `save-dashboard/manifest.json` -- step 6 manifest
- `end-journey/manifest.json` -- step 7 manifest

## Field Derivation Summary

### Path-level manifest

| Field | Source | Value |
|-------|--------|-------|
| id | directory name | `visualization-logs` |
| type | -- | `path` |
| description | recommender rule (dashboards-cloud.json) | "Step-by-step guide: Visualize logs in a Grafana Cloud dashboard." |
| category | website `_index.md` `journey.group` | `query-and-visualize` |
| author.name | CODEOWNERS (`/visualization-logs/` -> `@jstickler`) | `jstickler` |
| author.team | learning path convention | `Grafana Documentation` |
| startingLocation | first URL-bearing leaf from recommender match | `/dashboards` |
| targeting.match | recommender (2 rules combined with `or`) | see manifest |
| testEnvironment.tier | `targetPlatform: cloud` in match | `cloud` |
| milestones | website step `weight` ordering | 7 steps |
| depends | `_index.md` `related_journeys` (unambiguous prerequisite language) | `["drilldown-logs-lj"]` |
| recommends | `_index.md` `journey.links.to` | `["infrastructure-alerting-lj"]` |

### Step manifests

| Field | Source |
|-------|--------|
| id | each step's `content.json` `id` field |
| description | website step `index.md` `description` frontmatter |
| category | inherited from path `journey.group` |
| author.name | CODEOWNERS (`/visualization-logs/` -> `@jstickler`) |
| depends/recommends | step ordering by website `weight` |
| suggests | `side_journeys` items resolved to package IDs (where applicable) |

## Content Removed During Migration

- Removed image: `![Example logs visualization](/media/docs/learning-journey/logs-viz/logs-visualization-example.png)` -- website-relative image path
- Removed prerequisite: "A Grafana Cloud account. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)." -- redundant for Pathfinder users

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| dashboards-cloud.json | `https://grafana.com/docs/learning-journeys/visualization-logs/` | `{"and": [{"urlPrefix": "/dashboards"}, {"targetPlatform": "cloud"}]}` |
| dashboards-cloud.json | `https://grafana.com/docs/learning-journeys/visualization-logs/add-visualization/` | `{"and": [{"urlPrefix": "/dashboards"}, {"tag": "panel-type:logs"}, {"targetPlatform": "cloud"}]}` |

Note: The second rule targets a specific step URL but both rules were combined into the path-level targeting using an `or` wrapper since they represent distinct match contexts for the same learning path.

## Data Quality Issues

- **journeys.yaml vs `_index.md` mismatch:** `journeys.yaml` entry `visualize-logs` has `links.to: create-alert-logs`, but `_index.md` has `journey.links.to: infrastructure-alerting`. Used `_index.md` value as authoritative (`infrastructure-alerting-lj` for `recommends`). The website team should reconcile this inconsistency.
- All 7 steps had `pathfinder_data` present in their website markdown -- no directory name fallback was needed.
- No duplicate step descriptions detected.
- `side_journeys` items in steps are all external documentation links (not learning path URLs), so none were resolved to package IDs, except for `end-journey` which has a `related_journeys` item linking to `/docs/learning-paths/infrastructure-alerting/` -- resolved to `infrastructure-alerting-lj` in `suggests`.

## Surprises / Notes

- This directory does not follow the `-lj` naming convention despite being a learning path. The `id` is `visualization-logs` rather than `visualization-logs-lj`. This is a special case noted in the migration request.
- The `_index.md` `related_journeys.heading` says "This journey expects that you have completed the Drilldown Logs learning journey." -- this is unambiguous prerequisite language, so `drilldown-logs-lj` was placed in `depends` rather than `suggests`.
- The recommender has a second rule targeting the `add-visualization` step specifically (with an additional `tag: panel-type:logs` condition). Both rules were combined into the path-level targeting.
