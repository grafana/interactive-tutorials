---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: logql-101

## Files Created

- `manifest.json` — standalone guide manifest, type: "guide", no targeting (no index.json rule matched)

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `"logql-101"` |
| `type` | Constant | `"guide"` |
| `description` | Summarized from `content.json` title + content (no index.json rule) | `"Hands-on guide: Learn how to query, filter, and analyze logs using LogQL, Grafana Loki's query language."` |
| `category` | Default (not inside a *-lj directory) | `"general"` |
| `author.team` | Convention (standalone guide) | `"interactive-learning"` |
| `author.name` | Git log (`git log --follow`) | `"David Allen, Tom Glenn"` |
| `targeting` | Omitted — no matching index.json rule found | — |
| `startingLocation` | Omitted — no index.json rule, no URL derivable | — |
| `testEnvironment.tier` | Default (no match expression → minimum default) | `"cloud"` |
| `depends` | No data available | `[]` |
| `recommends` | No data available | `[]` |
| `suggests` | No data available | `[]` |
| `provides` | No data available | `[]` |

## No Targeting — Expected

No `index.json` rule exists for `logql-101`. This is not a migration error — targeting was simply not available to migrate. `targeting` and `startingLocation` are correctly omitted from the manifest.

## Flags for Manual Review

- **`description` synthesized**: The description was derived by summarizing the content.json (no index.json rule exists to supply a catalog-style description). Consider writing a more precise description and adding a matching index.json entry.
- **`category` defaulted**: Set to `"general"` because no parent `*-lj` directory provides a `journey.group`. If this guide belongs to a specific category (e.g., `"logs"`), update manually.

## Validator Result

`node dist/cli/cli/index.js validate --package logql-101` — **PASS** (✅)

Warnings (both expected per migration conventions):
- `WARN: "targeting" not specified` — expected, no index.json rule matched
- `WARN: "startingLocation" not specified, defaulting to "/"` — expected, manifest correctly omits it

## TODO

- [ ] Decide if `logql-101` should have contextual targeting (e.g., surface on `/explore` or `/a/grafana-lokiexplore-app`) and add an index.json rule if so
- [ ] Consider updating `category` from `"general"` to a more specific value (e.g., `"logs"`)
- [ ] Review synthesized `description` and replace with an authoritative catalog-style description if needed
