---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-09T00:00:00Z"
status: complete
---

# Migration Notes: run-first-k6-test-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 7 milestones, targeting from recommender)
- `content.json` — path-level cover page derived from website `_index.md`
- `understand-value/manifest.json` — step 1 manifest
- `install-k6/manifest.json` — step 2 manifest
- `write-test-script/manifest.json` — step 3 manifest
- `run-locally/manifest.json` — step 4 manifest
- `send-to-grafana-cloud/manifest.json` — step 5 manifest
- `analyze-results/manifest.json` — step 6 manifest
- `end-journey/manifest.json` — step 7 manifest

## Field Derivation Summary

### Path-level manifest

| Field | Source | Value |
|-------|--------|-------|
| `id` | Directory name | `run-first-k6-test-lj` |
| `type` | Convention | `path` |
| `description` | Recommender rule (testing-synthetics-cloud.json) | "Step-by-step guide: Install k6, write a test script, run it locally, and stream results to Grafana Cloud." |
| `category` | Website `_index.md` `journey.group` | `take-action` |
| `author.name` | Git history (10 commits) | `bonnywelsford-source` |
| `author.team` | Convention (learning path) | `Grafana Documentation` |
| `startingLocation` | Recommender match `urlPrefix` | `/a/k6-app` |
| `targeting.match` | Recommender rule (testing-synthetics-cloud.json) | `{"and": [{"urlPrefix": "/a/k6-app"}, {"targetPlatform": "cloud"}]}` |
| `testEnvironment.tier` | Recommender match (`targetPlatform: cloud`) | `cloud` |
| `milestones` | Website step `weight` ordering (100–700) | 7 steps |
| `recommends` | Website `_index.md` `journey.links.to` (absent) | `[]` |
| `suggests` | End-journey step `related_journeys` | `["detect-outages-synthetic-monitoring-lj", "create-availability-slo-lj"]` |

### Step-level manifests

| Field | Source | Notes |
|-------|--------|-------|
| `id` | Each step's `content.json` | Verified match between manifest and content.json |
| `description` (steps 1–6) | `index.json` rules | First priority per Description Conventions |
| `description` (end-journey) | Website `end-journey/index.md` frontmatter | No index.json entry for this step |
| `category` | Inherited from path (`take-action`) | All steps share path category |
| `author.name` | Git history | `bonnywelsford-source` (same as path) |
| `targeting` (steps 1–6) | `index.json` rules (step-level entries) | Included because steps have their own index.json entries |
| `targeting` (end-journey) | Omitted | No index.json entry for end-journey |
| `depends`/`recommends` | Step ordering (weight-based) | Chain: understand-value → install-k6 → write-test-script → run-locally → send-to-grafana-cloud → analyze-results → end-journey |
| `suggests` (end-journey) | Website `related_journeys` | `["detect-outages-synthetic-monitoring-lj", "create-availability-slo-lj"]` |

## Content Removed During Migration

Path-level `content.json` was derived from website `_index.md` with the following removals:

- Removed image: `![Example k6 test results dashboard showing performance metrics](/media/docs/k6-oss/screenshot-stream-k6-results-to-grafana-cloud-k6.png)` — website-relative image path
- Removed surrounding text: "The image below shows an example k6 test results dashboard displaying key performance metrics." — orphaned by image removal
- Removed prerequisite: `"- A Grafana Cloud account. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)."` — redundant for Pathfinder users
- Stripped Hugo shortcode: `{{< docs/box >}}` / `{{< /docs/box >}}` — preserved inner content
- Stripped Hugo shortcode: `{{< docs/icon-heading heading="## Here's what to expect" >}}` — extracted heading as markdown `## Here's what to expect`
- Removed markdown title: `# Run your first k6 performance test` — title field handles this

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| `testing-synthetics-cloud.json` | `https://grafana.com/docs/learning-paths/run-first-k6-test/` | `{"and": [{"urlPrefix": "/a/k6-app"}, {"targetPlatform": "cloud"}]}` |

Single recommender rule found. Type: `learning-journey`. No `or` wrapping needed.

## Data Quality Issues

- **No `pathfinder_data` on any step**: All 7 steps were mapped by directory name matching (website step directory names identical to interactive-tutorials step directories). Steps mapped: `understand-value`, `install-k6`, `write-test-script`, `run-locally`, `send-to-grafana-cloud`, `analyze-results`, `end-journey`.
- **No entry in `journeys.yaml`**: The path `run-first-k6-test` has no entry in `journeys.yaml`. No cross-validation of `journey.links.to` was possible.
- **Path `_index.md` has no `journey.links.to`**: The path-level `recommends` is `[]`. The `suggests` array was populated from the `end-journey` step's `related_journeys` frontmatter instead.
- **Path `_index.md` has no `related_journeys`**: Related journeys were found only in the `end-journey` step's website markdown.
- **Description priority resolutions** (expected, not conflicts): Steps 1–6 used index.json descriptions over website descriptions. The path used the recommender description over the `_index.md` description. These follow the defined priority cascade.
- **Step index.json entries with targeting**: Steps 1–6 each have their own `index.json` rule (type: `interactive`), which is unusual for learning path steps. Targeting was included on these step manifests per the skill rule "Omit targeting unless the step has its own index.json entry."
- **7 directories vs 6 index.json rules**: The `end-journey` directory has no `index.json` entry, which is expected for conclusion/summary steps.

## Validation Results

### Package validation (`validate --package`)

| Package | Result | Notes |
|---------|--------|-------|
| `run-first-k6-test-lj` | ✅ Pass | Schema defaults applied (repository, language, schemaVersion, conflicts, replaces) |
| `understand-value` | ✅ Pass | Schema defaults applied |
| `install-k6` | ✅ Pass | Schema defaults applied |
| `write-test-script` | ✅ Pass | Schema defaults applied |
| `run-locally` | ✅ Pass | Schema defaults applied |
| `send-to-grafana-cloud` | ✅ Pass | Schema defaults applied |
| `analyze-results` | ✅ Pass | Schema defaults applied |
| `end-journey` | ✅ Pass | Expected warnings: targeting not specified, startingLocation defaulted to "/" |

### Structural validation

- ✅ Every `manifest.json` has a matching `content.json` in the same directory
- ✅ `id` matches between each `manifest.json` and `content.json` pair
- ✅ No pre-existing `content.json` was modified (SHA-256 byte-level verification)
- ✅ `index.json` was not modified
- ✅ Recommender repo files were not modified
- ✅ Path manifest has `type: "path"` and a `milestones` array
- ✅ All step manifests have `type: "guide"`
- ✅ Step `depends`/`recommends` chains are consistent (no broken references within scope)
- ✅ All generated JSON is syntactically valid
- ✅ Milestone ordering matches website `weight` ordering (100→700)

## Surprises / Notes

- The `end-journey` step's `related_journeys` heading says "Consider taking the following paths after you complete this path" — clearly post-completion suggestions, placed in `suggests` (not `depends`).
- Both suggested paths (`detect-outages-synthetic-monitoring-lj` and `create-availability-slo-lj`) exist in the repository, so these are not dangling references.
- CODEOWNERS has no directory-specific entry for `/run-first-k6-test-lj/`. Author was derived from git history (`bonnywelsford-source`, 10 commits; `Chris Moyer`, 1 commit).
- The recommender rule uses `urlPrefix: "/a/k6-app"` while 4 of the step index.json entries use `urlPrefix: "/testing-and-synthetics"`. This means the path targets the k6 app specifically while most steps target the broader Testing & Synthetics section.
