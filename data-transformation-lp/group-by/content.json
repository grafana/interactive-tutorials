{
  "schemaVersion": "1.1.0",
  "id": "data-transformation-lp-group-by",
  "title": "Add a Group by transformation",
  "blocks": [
    {
      "type": "conditional",
      "conditions": [
        "renderer:website"
      ],
      "whenTrue": [
        {
          "type": "markdown",
          "content": "# Add a Group by transformation\n\n{{< docs/video id=\"g1DXq_IuJ7I\" align=\"right\" start=\"280\" end=\"333\" >}}\n\nThe group by transformation allows you to group rows that share the same values in one or more fields. It's similar to the `SQL GROUP by` clause. Use this when you need to aggregate data based on one or more fields, such as grouping by a specific category or region.\n\n{{< /docs/video >}}\n\nTo use the group by transformation, complete the following steps:\n\n1. Open your dashboard in edit mode.\n1. Click **Transformations** tab and click **+ Add transformation** button.\n1. Select or search for **Group by**.\n1. Select **Group by** on the dropdown of the field(s) that you want to group by.\n1. To add various calculations on the other fields and apply the calculation to each group, select **Calculate** and a calculation type on the dropdown of the field(s) that you want to calculate.\n1. The transformation should be applied automatically. If not, click **Refresh** to verify the visualization looks as intended.\n\n    The following image shows a group by transformation that groups all similar `Region` together and calculates the total `Sales amount` per region.\n\n    ![Sales revenue visualization using the Group by transformation](/media/docs/learning-journey/data-transformations/sales-after-transformation.png)\n\n1. Save your dashboard.\n"
        }
      ],
      "whenFalse": [
        {
          "type": "markdown",
          "content": "# Add a Group by transformation\n\nTo use the group by transformation, complete the following steps:\n\n1. Open your dashboard in edit mode.\n1. Click **Transformations** tab and click **+ Add transformation** button.\n1. Select or search for **Group by**.\n1. Select **Group by** on the dropdown of the field(s) that you want to group by.\n1. To add various calculations on the other fields and apply the calculation to each group, select **Calculate** and a calculation type on the dropdown of the field(s) that you want to calculate.\n1. The transformation should be applied automatically. If not, click **Refresh** to verify the visualization looks as intended.\n\n    The following image shows a group by transformation that groups all similar `Region` together and calculates the total `Sales amount` per region.\n\n1. Save your dashboard."
        }
      ]
    }
  ]
}
