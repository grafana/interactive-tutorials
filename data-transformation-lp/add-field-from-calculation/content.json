{
  "schemaVersion": "1.1.0",
  "id": "data-transformation-lp-add-field-from-calculation",
  "title": "Create an Add field from calculation transformation",
  "blocks": [
    {
      "type": "conditional",
      "conditions": [
        "renderer:website"
      ],
      "whenTrue": [
        {
          "type": "markdown",
          "content": "# Create an Add field from calculation transformation\n\n{{< docs/video id=\"g1DXq_IuJ7I\" align=\"right\" start=\"222\" end=\"279\" >}}\n\nThe add field from calculation transformation allows you to create a new column by performing a custom calculation on one or more existing fields in your data. Use this for when you need to create a new field, such as calculating the total, difference, percentage, or ratio.\n\n{{< /docs/video >}}\n\nTo use the add field from calculation transformation, complete the following steps:\n\n1. Open your dashboard in edit mode.\n1. Click **Transformations** tab and click **+ Add transformation** button.\n1. Select or search for **Add field from calculation**.\n1. Under **Mode**, select how you want your calculation to be applied. \n1. Use the following table as a guideline for when to use the different modes.\n\n    | Mode                 | When to use                                                                                        |\n    | ---------------------| -------------------------------------------------------------------------------------------------- |\n    | Binary               | When you want to apply basic binary operations on values in a single row from two selected fields. |\n    | Unary                | When you want to apply basic unary operations on values in a single row from a selected field.     |\n    | Reduce row           | When you want to apply selected calculation on each row of selected fields independently.          |\n    | Row index            | When you want to insert a field with the row index.                                                |\n    | Cumulative functions | When you want to apply functions on the current row and all preceding rows.                        |\n    | Window functions     | When you want to apply window functions.                                                           |\n\n1. For modes with the **Operation** or **Calculation** option, select the operation and calculation that you want to perform.\n1. Add a new name to the new field by updating **Alias**. \n1. The transformation should be applied automatically. If not, click **Refresh** to verify the visualization looks as intended.\n\n    The following image shows an add field from calculation transformation with a binary operation. The transformation calculates the `Average Views per User` by dividing the `Page views` with `Unique users`.\n\n    ![Website traffic visualization using the Add field from calculation transformation](/media/docs/learning-journey/data-transformations/website-traffic-after-add-field-calculation-transformation.png)\n\n1. Save your dashboard.\n"
        }
      ],
      "whenFalse": [
        {
          "type": "markdown",
          "content": "# Create an Add field from calculation transformation\n\nTo use the add field from calculation transformation, complete the following steps:\n\n1. Open your dashboard in edit mode.\n1. Click **Transformations** tab and click **+ Add transformation** button.\n1. Select or search for **Add field from calculation**.\n1. Under **Mode**, select how you want your calculation to be applied. \n1. Use the following table as a guideline for when to use the different modes.\n\n    | Mode                 | When to use                                                                                        |\n    | ---------------------| -------------------------------------------------------------------------------------------------- |\n    | Binary               | When you want to apply basic binary operations on values in a single row from two selected fields. |\n    | Unary                | When you want to apply basic unary operations on values in a single row from a selected field.     |\n    | Reduce row           | When you want to apply selected calculation on each row of selected fields independently.          |\n    | Row index            | When you want to insert a field with the row index.                                                |\n    | Cumulative functions | When you want to apply functions on the current row and all preceding rows.                        |\n    | Window functions     | When you want to apply window functions.                                                           |\n\n1. For modes with the **Operation** or **Calculation** option, select the operation and calculation that you want to perform.\n1. Add a new name to the new field by updating **Alias**. \n1. The transformation should be applied automatically. If not, click **Refresh** to verify the visualization looks as intended.\n\n    The following image shows an add field from calculation transformation with a binary operation. The transformation calculates the `Average Views per User` by dividing the `Page views` with `Unique users`.\n\n1. Save your dashboard."
        }
      ]
    }
  ]
}
