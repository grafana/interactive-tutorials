---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-17T20:06:46Z"
status: incomplete
---

# Migration Notes: instrumentation-hub-scale-lj

## Files Created

- `manifest.json` — path-level manifest (type: "path", 7 milestones)
- `content.json` — path-level cover page derived from welcome/content.json (no website _index.md available)
- `welcome/manifest.json` — step 1 manifest
- `why-scaling/manifest.json` — step 2 manifest
- `access-manage-tab/manifest.json` — step 3 manifest
- `open-namespace-settings/manifest.json` — step 4 manifest
- `enable-ongoing/manifest.json` — step 5 manifest
- `verify-ongoing/manifest.json` — step 6 manifest
- `end-journey/manifest.json` — step 7 manifest

## Field Derivation Summary

### Path-level manifest

| Field | Source | Value |
|-------|--------|-------|
| `id` | Directory name | `instrumentation-hub-scale-lj` |
| `type` | Convention | `path` |
| `description` | Summarized from welcome content.json | "Hands-on guide: Enable ongoing instrumentation in Grafana Instrumentation Hub to automatically capture new Kubernetes services." |
| `category` | Fallback (no website markdown) | `general` |
| `author.team` | Convention (learning path) | `Grafana Documentation` |
| `author.name` | Git history | `chri2547` |
| `testEnvironment.tier` | Default (no match expression) | `cloud` |
| `targeting` | Omitted — no recommender rules, no index.json entry | N/A |
| `startingLocation` | Omitted — no match expression to derive from | N/A |
| `depends` | welcome/content.json prerequisite reference | `["instrumentation-hub-discover-lj"]` |
| `recommends` | No website _index.md journey.links.to | `[]` |
| `suggests` | No website _index.md related_journeys | `[]` |
| `milestones` | Derived from content.json internal "next milestone" references | 7 steps in order |

### Step-level manifests (common fields)

| Field | Source | Value |
|-------|--------|-------|
| `type` | Convention | `guide` |
| `category` | Fallback (no website markdown) | `general` |
| `author.team` | Convention (learning path step) | `Grafana Documentation` |
| `author.name` | Git history | `chri2547` |
| `testEnvironment.tier` | Inherited from path (no match) | `cloud` |
| `targeting` | Omitted — steps inherit from path | N/A |

### Step descriptions

| Step | Description | Source |
|------|-------------|--------|
| welcome | Introduction to scaling instrumentation with Instrumentation Hub for automatic service capture. | Summarized from content.json title + body |
| why-scaling | Understand why scaling instrumentation is essential for growing Kubernetes environments. | Summarized from content.json title + body |
| access-manage-tab | Navigate to the Manage tab in Instrumentation Hub to view instrumented services. | Summarized from content.json title + body |
| open-namespace-settings | Open namespace settings to configure telemetry options for instrumented services. | Summarized from content.json title + body |
| enable-ongoing | Enable ongoing instrumentation so new services are automatically captured. | Summarized from content.json title + body |
| verify-ongoing | Verify that ongoing instrumentation is correctly enabled for your namespace. | Summarized from content.json title + body |
| end-journey | Review what you learned and explore next steps after enabling ongoing instrumentation. | Summarized from content.json title + body |

## Flags for Manual Review

- `category` defaulted to `"general"` — no website `_index.md` `journey.group` available. Reviewer should assign the correct category.
- `targeting` omitted — no recommender rules and no index.json entry found. If contextual recommendation is desired, targeting rules must be added to the recommender repo.
- `startingLocation` omitted — no match expression to derive from. If a starting page is known, add it manually.
- Step ordering derived from content.json internal "In the next milestone" references (no website `weight` field). Reviewer should verify order is correct.
- Path-level `content.json` derived from `welcome/content.json` (no website `_index.md` available). Reviewer should verify cover page content is appropriate.
- All step descriptions were summarized from content.json titles and body content (no website step `index.md` descriptions, no index.json rules). Reviewer should verify descriptions are appropriate.

## Content Removed During Migration

The path-level `content.json` was derived from `welcome/content.json` with the following content removed:

- Removed image: `![Instrumentation Hub showing discovered services](https://grafana.com/media/docs/grafana-cloud/instrumentation/ihub-first-page.png)` — website-relative image path that won't resolve in Pathfinder
- Removed prerequisite: "A Grafana Cloud account. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)." — redundant for Pathfinder users who are already in Grafana

## Dangling References

- `instrumentation-hub-discover-lj` in path `depends` — directory exists in this repo (confirmed), but it may not yet have a `manifest.json`. The Pathfinder CLI will catch this during validation.

## Recommender Rules

No recommender rules found for this path. Searched all `*.json` files in `grafana-recommender/internal/configs/state_recommendations/` for entries with `"type": "learning-journey"` and URL slug `instrumentation-hub-scale`. No matches.

This is expected for a brand-new learning path that hasn't been added to the recommender yet. Targeting rules should be added when the path is ready for contextual recommendation.

## Data Quality Issues

- No website learning path found at `content/docs/learning-paths/instrumentation-hub-scale/`. This path does not yet exist on the Grafana website. All metadata was derived from content.json files and fallback rules.
- No `journeys.yaml` entry found for this path.
- No CODEOWNERS entry specific to `/instrumentation-hub-scale-lj/`. Author derived from git history (Chris Moyer → chri2547).
- Step ordering derived from content.json internal "next milestone" text references, not from website `weight` fields. Steps mapped by directory name fallback (no `pathfinder_data` available).

## CLI Validation

CLI validation could not be run: `grafana-pathfinder-app/dist/cli/cli/index.js` does not exist (CLI not built). The `dist/` directory is absent in the pathfinder-app checkout. This must be resolved before the migration is complete.

## Surprises / Notes

- This learning path is brand new — no website markdown, no recommender rules, no index.json entries, no journeys.yaml entries. All metadata was derived from content.json files and convention-based fallbacks.
- The path has a clear prerequisite: `instrumentation-hub-discover-lj` (a sibling path that exists in this repo), referenced in the welcome content.json "Before you begin" section.
- The git history shows only one author (Chris Moyer / chri2547) with two commits.

## TODO

- [ ] TODO(review): `category` defaulted to `"general"` — reviewer should assign the correct category (e.g., from a future website `_index.md` `journey.group`)
- [ ] TODO(review): No targeting rules exist — reviewer should determine if recommender rules need to be created for contextual recommendation
- [ ] TODO(review): Step ordering derived from content.json cross-references, not from website `weight` — reviewer should verify order is correct
- [ ] TODO(review): Path-level `content.json` derived from `welcome/content.json` as fallback — reviewer should verify cover page content
- [ ] TODO(review): CLI validation not run — build the pathfinder CLI (`npm run build` in grafana-pathfinder-app) and run `node dist/cli/cli/index.js validate --package` against all packages
