---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-23T15:30:00Z"
status: incomplete
---

# Migration Notes: adaptive-metrics-lj

## Files Created

- `manifest.json` — path-level manifest (`type: "path"`, 8 milestones)
- `content.json` — path-level cover page (intro + "Before you begin")
- `value-adaptive-metrics/manifest.json` — step manifest
- `how-it-works/manifest.json` — step manifest
- `review-apply/manifest.json` — step manifest
- `rules-exemptions/manifest.json` — step manifest
- `segmentation/manifest.json` — step manifest
- `auto-apply/manifest.json` — step manifest
- `monitor-validate/manifest.json` — step manifest
- `end-journey/manifest.json` — step manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` (path) | directory name | `adaptive-metrics-lj` |
| `type` (path) | skill default for `*-lj` | `path` |
| `description` (path) | recommender rule (`adaptive-telemetry.json`, "Optimize your metrics with Adaptive Metrics") | "Step-by-step guide: Learn how to use Adaptive Metrics to automatically analyze and reduce unused or high-cardinality metrics." |
| `category` | **Fallback** — sibling `adaptive-logs-lj` uses `data-availability`; no website markdown available | `data-availability` |
| `author.team` | skill convention for `*-lj` | `Grafana Documentation` |
| `author.name` | git history (CODEOWNERS does not list `/adaptive-metrics-lj/`) | `Eve832` (from `81647476+Eve832@users.noreply.github.com`) |
| `targeting.match` | recommender rule (`adaptive-telemetry.json`, `type: "learning-journey"`) | copied verbatim |
| `startingLocation` | first URL-bearing leaf in match expression (`urlPrefixIn[0]`) | `/a/grafana-adaptive-metrics-app/overview` |
| `testEnvironment.tier` | `targetPlatform: "cloud"` in match expression | `cloud` |
| `milestones` (order) | existing `index.json` rule's `milestones` array order | value → how-it-works → review-apply → rules-exemptions → segmentation → auto-apply → monitor-validate → end-journey |
| Step `id` | each step's `content.json` `id` | as listed in milestones |
| Step descriptions | summarized from step `content.json` titles + content (no website markdown, no per-step `index.json` rules) | one-line catalog summaries |
| Step `depends`/`recommends` | sequential chain from milestones array | first step omits `depends`; last step omits `recommends` |

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| `../grafana-recommender/internal/configs/state_recommendations/adaptive-telemetry.json` | `https://grafana.com/docs/learning-journeys/adaptive-metrics/` | `and: [ urlPrefixIn: [/a/grafana-adaptive-metrics-app/{overview, rule-management, configuration}], targetPlatform: cloud ]` |

This is the only `type: "learning-journey"` rule in the recommender repo that resolves to `adaptive-metrics`. The same match expression also appears verbatim in this branch's `index.json` (rule "Learning Journey: Adaptive Metrics") — recommender wins per skill priority, but result is identical.

## Flags for Manual Review

- **`category` = `"data-availability"`** — adopted from sibling `adaptive-logs-lj` because the website markdown for the `adaptive-metrics` learning path was not available locally during migration. Verify against the website team's `journey.group` once available.
- **Path-level `content.json` body** — written from analogy to `adaptive-logs-lj/content.json` and the existing `value-adaptive-metrics/content.json` since no `_index.md` was available. Reviewer should expand or replace prose with authoritative website copy when accessible.
- **Step descriptions** — summarized rather than copied from website `index.md` `description` fields (none available). Reviewer should replace with authoritative descriptions from the Grafana website learning-paths content when it is accessible.

## Content Removed During Migration

None — no source markdown was available to strip from.

## Dangling References

None within this migration scope. All `depends`/`recommends` IDs point to sibling step manifests created in this same migration.

## Data Quality Issues

- The recommender rule URL (`/learning-journeys/adaptive-metrics/`) maps cleanly to `adaptive-metrics-lj` via the standard `-lj` stripping rule. No mapping ambiguity.
- The existing `index.json` "Learning Journey: Adaptive Metrics" rule on this branch contains a `milestones: [...]` block whose entries reference `id` values (`value-adaptive-metrics`, `how-it-works`, etc.) that **do not match** the actual step `content.json` `id` values (`adaptive-metrics-value`, `adaptive-metrics-how-it-works`, etc.). The new path-level `manifest.json` uses the `content.json` `id` values, which is the canonical source per the skill. Per Safety Invariant, `index.json` was not modified — the inconsistent `milestones` block in `index.json` is left for the author to address separately (or to delete, since LJ targeting now lives in `manifest.json`).

## Surprises / Notes

- **Pathfinder CLI validation was not run** because `node dist/cli/cli/index.js validate --package` was not available locally (pathfinder-app checkout not found at `~/hax/pathfinder-app`, `~/Documents/repositories/pathfinder-app`, or `../pathfinder-app`). This validation must be run by the author or in CI before merging.
- Website learning-paths markdown unavailable at the canonical path; all derivations that would normally come from `_index.md` and step `index.md` files used fallback rules.
- Initial state on disk had four step `content.json` files missing (auto-apply, monitor-validate, rules-exemptions, segmentation); restored from `git restore` before migration began.

## TODO

- [ ] TODO(fallback): `category` was set to `"data-availability"` by analogy with `adaptive-logs-lj` — verify against the website team's `journey.group` value for `adaptive-metrics` when website markdown becomes available.
- [ ] TODO(fallback): Step descriptions are summarized; replace with authoritative website `index.md` `description` values when available.
- [ ] TODO(review): Path-level `content.json` body is a minimal placeholder cover page modeled on `adaptive-logs-lj/content.json`. Reviewer should expand with authoritative learning-objectives prose from the website team's `_index.md` once accessible.
- [ ] TODO(review): Run `node dist/cli/cli/index.js validate --package adaptive-metrics-lj` (and one per step package) once the pathfinder-app CLI is available, to confirm schema compliance.
- [ ] TODO(review): The legacy `index.json` rule "Learning Journey: Adaptive Metrics" still contains a `milestones: [...]` block with stale `id` values. Consider removing the `milestones` block or the entire LJ rule from `index.json` now that targeting and milestones live in `adaptive-metrics-lj/manifest.json` (this is outside the migration skill's scope per Safety Invariants).
