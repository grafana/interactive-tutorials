---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-07T00:00:00Z"
status: complete
---

# Migration Notes: macos-integration-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 9 milestones, targeting from recommender)
- `content.json` — path-level cover page derived from website `_index.md`
- `business-value/manifest.json` — step 1 manifest
- `select-architecture/manifest.json` — step 2 manifest
- `install-alloy/manifest.json` — step 3 manifest
- `configure-alloy/manifest.json` — step 4 manifest
- `test-connection/manifest.json` — step 5 manifest
- `install-dashboards-alerts/manifest.json` — step 6 manifest
- `understand-dashboards/manifest.json` — step 7 manifest
- `understand-alerts/manifest.json` — step 8 manifest
- `end-macos/manifest.json` — step 9 manifest

## Field Derivation Summary

### Path-level manifest

| Field | Source | Value |
|-------|--------|-------|
| id | directory name | `macos-integration-lj` |
| type | — | `path` |
| description | recommender rule (connections-cloud.json) | "Step-by-step guide: Monitor a macOS system in Grafana Cloud." |
| category | website `_index.md` journey.group | `data-availability` |
| author.name | CODEOWNERS | `chri2547` |
| author.team | convention | `Grafana Documentation` |
| startingLocation | recommender match (first urlPrefixIn entry) | `/connections/add-new-connection/macos-node` |
| targeting.match | recommender rule (connections-cloud.json) | `and: [urlPrefixIn [...], targetPlatform: cloud]` |
| testEnvironment.tier | match contains targetPlatform: cloud | `cloud` |
| milestones | website step weight ordering | 9 steps |
| recommends | `_index.md` journey.links.to | `fleet-mgt-monitor-health-lj`, `drilldown-metrics-lj`, `drilldown-logs-lj` |

### Step manifests

| Field | Source |
|-------|--------|
| id | each step's content.json |
| description | website step index.md frontmatter description (condensed where needed) |
| category | inherited from path (`data-availability`) |
| author.name | CODEOWNERS (`chri2547`) |
| depends/recommends | step ordering by weight |
| suggests (end-macos) | related_journeys from end-macos index.md |

## Dangling References

- `fleet-mgt-monitor-health-lj` — referenced in path recommends and end-macos suggests; directory does not exist in this repo (expected — not yet migrated or different naming)

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| connections-cloud.json | `https://grafana.com/docs/learning-journeys/macos-integration/` | `and: [urlPrefixIn: ["/connections/add-new-connection/macos-node", "/connections/infrastructure"], targetPlatform: "cloud"]` |

## Data Quality Issues

- **journeys.yaml vs _index.md ID mismatch**: journeys.yaml uses `monitor-macos` with links to `monitor-collector-health`, `metrics-drilldown`, `logs-drilldown`. The `_index.md` uses `journey.links.to`: `fleet-mgt-monitor-health`, `drilldown-metrics`, `drilldown-logs`. Used `_index.md` values as authoritative (they map to actual directory names). The website team should reconcile the naming inconsistency.
- All steps had `pathfinder_data` frontmatter — no directory-name-only fallback was needed.
- No duplicate step descriptions detected.

## Validation Results

- `validate --package macos-integration-lj`: PASS
- All 9 step packages: PASS
- Step-level warnings about missing `targeting` and `startingLocation` are expected (targeting lives at path level).
- All existing content.json files verified unmodified (SHA-256 comparison).
