# Migration Notes: grafana-13-tour-learn

Scaffold for the Grafana 13 feature tour on **Learn Grafana** (`learn.grafana.net`). Mirrored from `grafana-13-tour-play`; update dashboard `reftarget` paths if Learn uses different UIDs than Play, and adjust `manifest.json` targeting (`startingLocation`, `targeting.match`) if the entry dashboard changes.

Validate with your packaging workflow when ready (for example `--package` if your team uses the migrate-guide tooling).
