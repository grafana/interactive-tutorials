---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: rca-demo-ops

## Files Created

- `manifest.json` — standalone guide manifest derived from content.json id/title and git history; no index.json rule matched.

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `rca-demo-ops` |
| `type` | constant | `"guide"` |
| `description` | summarized from content.json title + body intro | `"Hands-on demo: Walk through an end-to-end root cause analysis using Grafana's RCA Workbench, from alert to blast radius and business impact."` |
| `category` | default (not inside a *-lj directory) | `"general"` |
| `author.team` | default (standalone guide) | `"interactive-learning"` |
| `author.name` | git log `--follow` on content.json | `"Tom Glenn"` |
| `startingLocation` | omitted — no index.json rule | (omitted) |
| `targeting` | omitted — no index.json rule | (omitted) |
| `testEnvironment.tier` | default (no match expression) | `"cloud"` |
| `depends` / `recommends` / `suggests` / `provides` | none available | `[]` |

## No Targeting — Expected

No `index.json` rule exists for `rca-demo-ops`. This is not a migration error — targeting was simply not available to migrate. `targeting` and `startingLocation` are correctly omitted from the manifest.

## Flags for Manual Review

- **`description`**: No matching `index.json` rule was found, so the description was derived by summarizing the guide's title and introductory content block. A product owner should verify it is accurate for catalog display.
- **`category`**: Defaulted to `"general"` because the guide is not inside a `*-lj` directory. If it belongs to a specific product category (e.g., `"incident-management"` or `"observability"`), update this field.

## Validator Result

Command: `node /Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js validate --package /Users/davidallen/hax/interactive-tutorials/rca-demo-ops`

Result: PASS (exit 0)

Warnings (expected):
- `manifest.json: "targeting" not specified` — expected; no index.json rule exists.
- `manifest.json: "startingLocation" not specified, defaulting to "/"` — expected; manifest correctly omits this field.

## Surprises / Notes

- No `index.json` rule matched `rca-demo-ops`. The guide appears to be demo/ops content not yet registered for contextual recommendation.
- Git log shows a single non-bot author (Tom Glenn) across all three commits for this file.
- The `rca-demo-ops` directory contained only `content.json` and `unstyled.html` — no pre-existing `manifest.json` or `assets/` directory.

## TODO

- [ ] Decide whether to add a `targeting` rule to `index.json` so this guide can be contextually recommended (e.g., from `/a/grafana-asserts-app/assertions`).
- [ ] Confirm or update `description` with the product/content team.
- [ ] Consider assigning a more specific `category` value once the category taxonomy is finalized.
