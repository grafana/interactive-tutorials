{
  "id": "deploy-alert-terraform-create-service-account",
  "title": "Create a service account token",
  "blocks": [
    {
      "type": "markdown",
      "content": "Terraform needs an API credential to authenticate to your Grafana instance. A service account token provides this without using your personal login. It's a dedicated credential for automated tools.\n\nTo create a service account token, complete the following steps:\n\n1. Log in to your Grafana instance with an Admin account.\n\n1. Navigate to **Administration > Users and access > Service accounts** in the left-side menu.\n\n1. Click **Add service account**.\n\n1. Enter a name for the service account, for example `terraform-deployer`.\n\n1. Set the role to **Editor** (sufficient for creating and updating alerting resources and folders).\n\n1. Click **Create**.\n\n1. On the service account detail page, click **Add service account token**.\n\n1. Enter a token name, for example `terraform-token`.\n\n1. Click **Generate token**.\n\n1. Copy the generated token and store it securely. You will not be able to see it again."
    },
    {
      "type": "markdown",
      "content": "> **Warning:** Never commit service account tokens to version control. Store them as environment variables or in a secrets manager."
    },
    {
      "type": "markdown",
      "content": "## What you need for the next steps\n\n| Value | Example | Where it comes from |\n|-------|---------|-------------------|\n| **Grafana URL** | `https://your-stack.grafana.net/` | Your Grafana instance URL |\n| **Service account token** | `glsa_xxxxxxxxxxxx` | The token you just generated |\n\nKeep both values accessible. You'll use them to configure the Terraform provider in the next milestone.\n\nIn the next milestone, you configure the Grafana Terraform provider with these credentials."
    }
  ]
}
