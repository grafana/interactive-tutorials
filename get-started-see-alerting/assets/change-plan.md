---
disclaimer: Auto-generated by update-guide skill. Do not edit manually.
notice: To regenerate, re-run the skill with the same change request.
input_sha256: 3d494eda66ad2f145752a5902d59630accb911dae36f05b412b9f67c20514d1b
guide_id: get-started-see-alerting
change_request: "Show users a dashboard or wherever alerts are firing so they can see alerting in action."
---

# Change Plan

## Request

Show users a dashboard or wherever alerts are firing so they can see alerting in action.

## Recommendation

Use **Alert activity** (`/alerting/alerts`) as the firing view, not a dashboard.

Verified live on Grafana Play (anonymous, 2026-08-27):

- **Alert activity** loads with the **Firing** filter already on. The workbench listed 31 firing alerts, including the **Demo: Alerting** folder and **Random Single Serie**.
- The **Demo: Alerting** dashboard folder (`/dashboards/f/demo-alerting-examples/demo-alerting`) is empty: 0 dashboards, 9 alert rules. There is no Play dashboard here to open.

Keep the 3–6 read-only step budget. Swap the current list → group → rule clicks for Alert activity, then still open **Random Single Serie** by URL to show the query and the condition. Step count stays at 5.

## Edits

- **Action**: edit
- **Path**: `blocks[0]`
- **Block type**: markdown
- **What it does**: Rewrite the learning objective so the learner will see firing alerts on Alert activity, then open one rule to see the question and the line.
- **Why this location**: Guide intro; must match the new first section.

- **Action**: edit
- **Path**: `blocks[1]`
- **Block type**: markdown
- **What it does**: Change the pre-section intro to "First, see alerts that are firing:"
- **Why this location**: Section bookend (rule 14) immediately before the first section.

- **Action**: replace
- **Path**: `blocks[2]`
- **Block type**: section
- **What it does**: Replace `find-a-rule` with section `see-whats-firing` (title: "See what's firing"). Two steps: `navigate` to `/alerting/alerts`, then `highlight` (`doIt: false`) the workbench. Keep the section `id` as `find-a-rule` unless you approve a rename to `see-whats-firing` (nothing external references the id).
- **Why this location**: Same slot as today's first section; this is the "in action" beat.

Proposed section steps:

1. `navigate` `/alerting/alerts`, `verify`/`objectives` `on-page:/alerting/alerts`. Copy: open **Alert activity**. This page lists alerts that have crossed their line. Play already filters it to **Firing**.
2. `highlight` `doIt: false` `[data-testid='data-testid triage groups-container']`. Copy: each row is an alert firing right now, grouped in folders. **Demo: Alerting** is Play's demo set.

- **Action**: edit
- **Path**: `blocks[3]`
- **Block type**: markdown
- **What it does**: Post-section summary: you saw firing alerts. Firing means the line was crossed, and Alert activity is where you look.
- **Why this location**: Section bookend (rule 14) immediately after the first section.

- **Action**: edit
- **Path**: `blocks[4]`
- **Block type**: markdown
- **What it does**: Keep "Next, read what this rule watches:" (or "Next, open one rule to see why it can fire:").
- **Why this location**: Existing intro to the second section.

- **Action**: edit
- **Path**: `blocks[5]` (`read-the-rule`)
- **Block type**: section
- **What it does**: Insert a `navigate` to `/alerting/grafana/random-single-serie` as the first step (the group/rule clicks go away, so this is how the learner opens the rule). Drop the section-level `on-page:/alerting/grafana/random-single-serie` requirement so the navigate is eligible. Keep the query and condition highlights unchanged.
- **Why this location**: Same rule as today; URL navigate is more reliable than clicking a firing row (Random Single Serie is a random walk and is not always in the firing list).

- **Action**: edit
- **Path**: `blocks[7]`
- **Block type**: markdown
- **What it does**: Closing "What you have learned" names both beats: firing shows up on Alert activity; a rule is a question plus a line.
- **Why this location**: Guide closer.

- **Action**: edit
- **Path**: `manifest.json` (`startingLocation`, `description`)
- **Block type**: manifest
- **What it does**: Set `startingLocation` to `/alerting/alerts`. Rewrite `description` to mention Alert activity (firing alerts) plus opening one rule.
- **Why this location**: Guide now starts on Alert activity; targeting `urlPrefix: /alerting` + Play is unchanged.

## New Selectors To Verify

- `navigate` `/alerting/alerts` — Alert activity; Play applies the Firing filter by default
- `[data-testid='data-testid triage groups-container']` — workbench listing firing rules (verified present 2026-08-27)
- `[data-testid='data-testid triage state-filter firing']` — Firing chip in the sidebar (backup highlight if the workbench selector is unstable)
- `navigate` `/alerting/grafana/random-single-serie` — existing rule, already used

## New Requirements / Objectives

- First section: `on-page:/alerting/alerts` on the navigate (`verify` + `objectives`); `exists-reftarget` on the workbench highlight; `on-page:/alerting/alerts` on that highlight
- Second section: `on-page:/alerting/grafana/random-single-serie` on the new navigate (`verify` + `objectives`); existing query/condition highlights keep `on-page:/alerting/grafana/random-single-serie` + `exists-reftarget`
- Drop section-level `requirements` on `read-the-rule` so the new navigate can run (rule 11: later highlights still require the page)

## IDREFs Affected

None. No `section-completed:` or `var-` references. Section id `find-a-rule` is unused outside this file. Proposed keep-the-id default; rename only if you want the id to match the new title.

## Risks

- **No dashboard to open.** Demo: Alerting has zero dashboards. A dashboard beat would be a dead end unless we pick a different Play dashboard with an Alert list panel (not verified).
- **Firing count fluctuates.** Do not hardcode "31". Random Single Serie may leave the firing list; that is why the rule opens by URL, not by clicking a workbench row.
- **Play's default Firing filter** is current behavior (the `/alerting/alerts` load added `var-filters=alertstate|=|firing`). If that default goes away, the workbench still shows alert activity; copy should not claim the page shows *only* firing unless we also highlight the Firing chip.
- **Workbench virtualisation (rule 21).** Six groups were in the DOM at load. Targeting the container, not a row below the fold, so a plain `interactive` highlight should be enough. If `exists-reftarget` fails in review, fall back to the Firing chip (always above the fold) or wrap the workbench step in `guided` + `lazyRender: true`.
- **Website slide is out of scope for this skill.** `06d-alerting/index.md` still says "the alert rule list, one open rule." After this guide change, that sentence should mention Alert activity. Same for `outline.md` / `doc-facts.md`. Confirm if you want those updated in the same pass.
- **Step budget.** 5 read-only steps, still inside the 3–6 spec. Adding both the workbench *and* the Firing chip would make 6, which is still allowed.

## Critical rules considered

- 1 navmenu-open: not needed; Alerting submenu is collapsed, so `navigate` (same pattern as today's `/alerting/list`)
- 2 stable selectors: `data-testid` for the workbench
- 5 exists-reftarget on selector-targeting steps
- 7 on-page for page-specific actions
- 9–10 brief, action-focused copy
- 14 section bookends stay outside the section
- 17 doIt false on highlight-only workbench step
- 21 lazyRender only if we target a virtualised row; not needed for the container
