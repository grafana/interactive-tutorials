---
disclaimer: Auto-generated by update-guide skill. Do not edit manually.
notice: To regenerate, re-run the skill with the same change request.
input_sha256: 5b6ec10a6af1e7dcecbdeaab26952373a19854ab915ebb8038ca74657b4516c0
guide_id: get-started-see-alerting
change_request: "Apply review ideas: notification beat, Firing chip step, tooltip fix, section tie-in, narrative frame"
---

# Change Plan

## Request

Apply all four review ideas from the 2026-08-27 review, plus two additions from the user:

1. Close the notification loop (the guide claims "a notification goes out" but never shows it).
2. Make the "already filtered to Firing" claim visible by highlighting the Firing chip.
3. Fix the condition tooltip's direction claim.
4. Tie section 2 back to section 1 with connective copy.
5. Spelling: user believed "Random Single Serie" should be "Random Single Series" (UI clipping). **Verified live: the rule's actual display name is "Random Single Serie"** — h1, breadcrumb, and page title all match. Keep the name as-is.
6. Add a narrative frame: a number periodically goes off the rails; a rule watches it so nobody has to. Cast the learner as reading an existing rule (Play is read-only for anonymous users — the story must not claim the learner creates anything).

User explicitly allowed exceeding the 6-step budget. New total: 7 interactive steps.

## Live verification (play.grafana.org, anonymous, 2026-08-27 ~15:40 CT)

- `/alerting/alerts` loads with `var-filters=alertstate|=|firing` applied; h1 "Alert activity".
- `[data-testid='data-testid triage state-filter firing']` exists — chip text "Firing 32". A "Pending 1" chip also exists.
- `[data-testid='data-testid triage groups-container']` exists.
- Rule view h1: **Random Single Serie** (no trailing "s" — the Play rule is genuinely named this).
- `[data-testid='queries-container']` and `[data-testid='expressions-container']` exist. Condition verified: Threshold, **Is above 75**; evaluation interval **Every 3m**; current state Normal.
- `[data-testid='data-testid Tab Notifications']` exists. Clicking it shows a sent-notification log (filters: Status, Outcome, Contact point, Last 30 days; currently "No notifications found"). Copy must describe it as the *record* of sent notifications, not the routing config.

## Edits

- **Action**: edit — **Path**: `blocks[0]` — markdown. Rewrite intro with the narrative setup (drifting number, nobody wants to babysit it, a rule watches instead) while keeping the "question + line" definition. Why: the story must start before the first section.
- **Action**: edit — **Path**: `blocks[1]` — markdown. Minor: "First, see alerts that are firing right now:". Why: bookend (rule 14).
- **Action**: edit — **Path**: `blocks[2].blocks[0]` — interactive navigate. Reword: Alert activity is where you look when you want to know what's wrong right now. Why: move the "everything here crossed its line" claim onto the Firing-chip step, which shows the evidence.
- **Action**: add — **Path**: `blocks[2].blocks[1]` (new, before groups highlight) — interactive highlight, `doIt: false`, Firing chip. Why: idea 2 — makes the filter claim self-evidencing.
- **Action**: edit — **Path**: `blocks[2].blocks[2]` (was blocks[2].blocks[1]) — groups-container highlight. Add tie-in: the rule you're about to meet lives in **Demo: Alerting**. Why: idea 4 setup.
- **Action**: edit — **Path**: `blocks[3]` — markdown bookend after section 1. Keep substance.
- **Action**: edit — **Path**: `blocks[4]` — markdown. "Next, meet the rule that does the watching:". Why: narrative bridge.
- **Action**: edit — **Path**: `blocks[5].blocks[0]` — navigate to rule. Tie-in copy: one of the demo rules from the folder you just saw; the watcher from the story. Why: idea 4.
- **Action**: edit — **Path**: `blocks[5].blocks[1]` — queries highlight. Ground "every few minutes" (verified Every 3m); tie to the drifting number. Why: narrative.
- **Action**: edit — **Path**: `blocks[5].blocks[2]` — expressions highlight. Content may keep direction ("climbs above" — verified Is above 75); tooltip goes direction-neutral ("cross it and the rule fires"). Why: idea 3, robustness if Play re-provisions.
- **Action**: add — **Path**: `blocks[5].blocks[3]` (new) — interactive highlight, `doIt: false`, Notifications tab. Copy: the rule sends a message to a person or channel; this tab keeps the record of every send. Why: idea 1, closes the notification loop.
- **Action**: edit — **Path**: `blocks[6]` — closing markdown. Narrative payoff (3 a.m., nobody watching) + both beats + notification. Why: story close.
- **Action**: edit — **Path**: `manifest.json` (`description`). Mention the notification beat. `startingLocation` unchanged.
- **Action**: edit — **Path**: website `06d-alerting/index.md` (out of repo, companion slide). Extend the walk-through sentence to include where notifications go. Why: slide promised notifications; guide now shows them.

## New Selectors To Verify

All verified live (see above):

- `[data-testid='data-testid triage state-filter firing']`
- `[data-testid='data-testid Tab Notifications']`

## New Requirements / Objectives

- Firing chip step: `on-page:/alerting/alerts` + `exists-reftarget`
- Notifications tab step: `on-page:/alerting/grafana/random-single-serie` + `exists-reftarget`

No new objectives; both new steps are `doIt: false` tour highlights.

## IDREFs Affected

None. No `section-completed:` or `var-` references exist.

## Risks

- **Threshold direction**: content says the rule fires when the answer climbs above the line (verified Is above 75 today). If Play re-provisions the rule with a different direction, this clause goes stale. Tooltip is direction-neutral as a hedge. Threshold value 75 is deliberately NOT hardcoded.
- **Firing chip count**: copy never hardcodes the count (32 at verification; fluctuates).
- **Notifications tab is empty today** ("No notifications found" for this rule, last 30 days). Copy describes the tab as the record of sends without claiming any are visible.
- **Rule name**: "Random Single Serie" is the genuine Play name. If Play ever renames it to "…Series", the bolded name and the URL slug both need updating (slug would break first).

## Critical rules considered

- 2 stable selectors: both new targets use `data-testid`
- 5 `exists-reftarget` on both new selector steps
- 7 `on-page:` on both new steps
- 9-10 brief, action-focused copy
- 12 tooltips: one sentence, <250 chars, never name the element
- 14 bookends stay outside sections
- 15 bold only GUI names (**Alert activity**, **Firing**, **Demo: Alerting**, **Random Single Serie**, **Notifications**, **alert condition** — the last is a literal UI label in the expressions panel, verified)
- 17 `doIt: false` on all highlight-only steps
- 21 lazyRender: not needed — chip and tab are above the fold; containers verified present at load
