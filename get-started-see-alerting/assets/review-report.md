---
disclaimer: Auto-generated by update-guide skill. Do not edit manually.
notice: To regenerate, re-run the skill with the same change request.
input_sha256: 3d494eda66ad2f145752a5902d59630accb911dae36f05b412b9f67c20514d1b
guide_id: get-started-see-alerting
change_request: "Show users a dashboard or wherever alerts are firing so they can see alerting in action."
---

# Review Report

## Summary

- Findings: 0 (auto-fixed: 0, requires user attention: 0)
- Scope: `blocks[0]`–`blocks[6]`, `blocks[2].blocks[0]`–`blocks[2].blocks[1]`, `blocks[5].blocks[0]`–`blocks[5].blocks[2]`, `manifest.json`

## Blocking checks (review-guide-pr.mdc §1)

- 1.1 navmenu-open: no nav-menu selectors; both new steps use `navigate` (Alerting submenu is collapsed on Play).
- 1.2 fragile selectors: workbench uses `data-testid`; rule still uses existing `queries-container` / `expressions-container`.
- 1.3 no multistep singletons.
- 1.4 no focus-before-formfill; workbench highlight is `doIt: false`.
- 1.5 `on-page:/alerting/alerts` on the workbench highlight; `on-page:/alerting/grafana/random-single-serie` on query/condition highlights.
- 1.6 no leading markdown title.
- 1.7 no popout.
- 1.8 workbench target is the groups container (in DOM at load, above the fold on Play). Not a virtualised row. Plain `interactive` is enough.

## Structural (§2.4–2.5) and §4.6

- Section ids unique (`find-a-rule`, `read-the-rule`).
- Bookends sit outside sections.
- No `?doc=` / `openGuide` chain.
- Query and condition blocks are content-identical to the snapshot (path-shifted after the new navigate).

## Auto-fixed

None.

## Requires user attention

None.
