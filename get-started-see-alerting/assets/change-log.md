---
disclaimer: Auto-generated by update-guide skill. Do not edit manually.
notice: To regenerate, re-run the skill with the same change request.
input_sha256: 3d494eda66ad2f145752a5902d59630accb911dae36f05b412b9f67c20514d1b
guide_id: get-started-see-alerting
change_request: "Show users a dashboard or wherever alerts are firing so they can see alerting in action."
---

# Change Log

## Edit 1 — edit `blocks[0]`

**Before**:
```json
{ "type": "markdown", "content": "**What you will learn:** An alert rule is a question asked on a schedule, with a line that must not be crossed. You'll find the rule list and open one real rule to see the question and the line." }
```

**After**:
```json
{ "type": "markdown", "content": "**What you will learn:** An alert rule is a question asked on a schedule, with a line that must not be crossed. You'll see alerts that are firing right now, then open one rule to see the question and the line." }
```

## Edit 2 — edit `blocks[1]`

**Before**:
```json
{ "type": "markdown", "content": "First, find a rule worth opening:" }
```

**After**:
```json
{ "type": "markdown", "content": "First, see alerts that are firing:" }
```

## Edit 3 — replace `blocks[2]` and children `blocks[2].blocks[0]`, `blocks[2].blocks[1]`, `blocks[2].blocks[2]`

**Before**: section `find-a-rule` titled "Find a rule" with navigate `/alerting/list`, highlight group, highlight **Random Single Serie**.

**After**: section `find-a-rule` titled "See what's firing" with navigate `/alerting/alerts` and highlight (`doIt: false`) `[data-testid='data-testid triage groups-container']`. Child `blocks[2].blocks[2]` removed.

## Edit 4 — edit `blocks[3]`

**Before**:
```json
{ "type": "markdown", "content": "You navigated list, group, rule: the same three levels every alert in Grafana lives in." }
```

**After**:
```json
{ "type": "markdown", "content": "You saw alerts in action: firing means the line was crossed, and **Alert activity** is where you look." }
```

## Edit 5 — edit `blocks[4]`

**Before**:
```json
{ "type": "markdown", "content": "Next, read what this rule watches:" }
```

**After**:
```json
{ "type": "markdown", "content": "Next, open one rule to see why it can fire:" }
```

## Edit 6 — edit `blocks[5]`

Dropped section-level `requirements: ["on-page:/alerting/grafana/random-single-serie"]` so the new navigate can run.

## Edit 7 — add `blocks[5].blocks[0]`

**Before**: (none — new block; previous `blocks[5].blocks[0]` shifted to `blocks[5].blocks[1]`)

**After**:
```json
{
  "type": "interactive",
  "action": "navigate",
  "reftarget": "/alerting/grafana/random-single-serie/view",
  "verify": "on-page:/alerting/grafana/random-single-serie",
  "objectives": ["on-page:/alerting/grafana/random-single-serie"],
  "content": "Open **Random Single Serie** to see the question and the line."
}
```

## Edit 8 — path shift `blocks[5].blocks[1]` (was `blocks[5].blocks[0]`)

Content unchanged: query highlight (`queries-container`).

## Edit 9 — path shift `blocks[5].blocks[2]` (was `blocks[5].blocks[1]`)

Content unchanged: condition highlight (`expressions-container`).

## Edit 10 — edit `blocks[6]`

**Before**:
```json
{ "type": "markdown", "content": "**What you have learned:** A rule is a question plus a line: a query asked on a schedule, and a condition that decides when to fire. Grafana keeps watch so you don't have to." }
```

**After**:
```json
{ "type": "markdown", "content": "**What you have learned:** Firing alerts show up on **Alert activity**. A rule is a question plus a line: a query asked on a schedule, and a condition that decides when to fire. Grafana keeps watch so you don't have to." }
```

## Edit 11 — edit `manifest.json`

- `startingLocation`: `/alerting/list` → `/alerting/alerts`
- `description`: now names Alert activity (firing alerts) plus opening one rule
