---
disclaimer: Auto-generated by update-guide skill. Do not edit manually.
notice: To regenerate, re-run the skill with the same change request.
input_sha256: 5b6ec10a6af1e7dcecbdeaab26952373a19854ab915ebb8038ca74657b4516c0
guide_id: get-started-see-alerting
change_request: "Apply review ideas: notification beat, Firing chip step, tooltip fix, section tie-in, narrative frame"
---

# Change Log

## Edit 1 — edit `blocks[0]` (intro markdown)

**Before**: "**What you will learn:** An alert rule is a question asked on a schedule, with a line that must not be crossed. You'll see alerts that are firing right now, then open one rule to see the question and the line."

**After**: Adds the narrative setup (a number on Play drifts, wanders too high, a rule watches it) and extends the promise to include where the rule's message goes.

## Edit 2 — edit `blocks[1]` (pre-section bookend)

**Before**: "First, see alerts that are firing:"

**After**: "First, see alerts that are firing right now:"

## Edit 3 — edit `blocks[2].blocks[0]` (navigate to Alert activity)

**Before**: "Open **Alert activity**. This page lists alerts that have crossed their line. Play already filters it to **Firing**."

**After**: "Open **Alert activity**. This is where you look when you want to know what's wrong right now." (The filter claim moved to the new Firing-chip step, which shows the evidence.)

## Edit 4 — add `blocks[2].blocks[1]` (Firing chip highlight)

**Before**: (none — new block)

**After**:
```json
{
  "type": "interactive",
  "action": "highlight",
  "doIt": false,
  "reftarget": "[data-testid='data-testid triage state-filter firing']",
  "content": "The **Firing** filter is already on, so every alert listed has crossed its line. The count rises and falls as rules fire and recover.",
  "tooltip": "A live tally of crossed lines: it climbs when rules fire and drops when the numbers come back down.",
  "requirements": ["on-page:/alerting/alerts", "exists-reftarget"]
}
```

## Edit 5 — edit `blocks[2].blocks[2]` (groups-container highlight, was blocks[2].blocks[1])

**Before**: "Each row is an alert firing right now, grouped in folders. **Demo: Alerting** is Play's demo set."

**After**: "Each row is one alert firing right now, grouped in folders. **Demo: Alerting** is Play's demo set — the rule you're about to meet lives there." (Tooltip and requirements unchanged.)

## Edit 6 — edit `blocks[4]` (pre-section-2 bookend)

**Before**: "Next, open one rule to see why it can fire:"

**After**: "Next, meet the rule that does the watching:"

## Edit 7 — edit `blocks[5].blocks[0]` (navigate to rule)

**Before**: "Open **Random Single Serie** to see the question and the line."

**After**: "Open **Random Single Serie**, one of the demo rules from the folder you just saw. This is the watcher from our story: it guards a number that drifts on its own." (reftarget/verify/objectives unchanged. Rule name verified live — Play's rule is genuinely named "Random Single Serie".)

## Edit 8 — edit `blocks[5].blocks[1]` (queries highlight)

**Before**: "This is the question: a query the rule asks on a schedule, with the answer drawn as a chart."

**After**: "This is the question: a query the rule asks every few minutes, with the answer drawn as a chart. That's the drifting number, checked on a schedule." ("Every few minutes" grounded by the rule's verified evaluation interval, Every 3m. Tooltip unchanged.)

## Edit 9 — edit `blocks[5].blocks[2]` (condition highlight)

**Before**: content "…When the answer crosses it, the rule fires and a notification goes out." / tooltip "The threshold that turns a number into a decision: above the line means fire, below means stay quiet."

**After**: content "This is the line: the **alert condition**. This rule fires when the answer climbs above its threshold — the moment \"too high\" stops being an opinion." (direction verified live: Threshold, Is above 75) / tooltip "The threshold that turns a number into a decision: cross it and the rule fires." (direction-neutral hedge). The notification claim moved to the new Notifications step.

## Edit 10 — add `blocks[5].blocks[3]` (Notifications tab highlight)

**Before**: (none — new block)

**After**:
```json
{
  "type": "interactive",
  "action": "highlight",
  "doIt": false,
  "reftarget": "[data-testid='data-testid Tab Notifications']",
  "content": "Firing isn't the end of the story: the rule sends a message to a person or channel, so someone can act. The **Notifications** tab keeps the record of every message this rule has sent.",
  "tooltip": "When the line is crossed, Grafana tells someone — and the record of every send lands here.",
  "requirements": ["on-page:/alerting/grafana/random-single-serie", "exists-reftarget"]
}
```

## Edit 11 — edit `blocks[6]` (closer markdown)

**Before**: "**What you have learned:** Firing alerts show up on **Alert activity**. A rule is a question plus a line: a query asked on a schedule, and a condition that decides when to fire. Grafana keeps watch so you don't have to."

**After**: Narrative payoff: the drifting number can go off the rails at 3 a.m.; the rule asks the question, checks the line, sends the message; the firing alert lands on **Alert activity**.

## Edit 12 — edit `manifest.json` (description)

**Before**: "…the query it asks and the condition that makes it fire."

**After**: "…the query it asks, the condition that makes it fire, and the tab that records the notifications it sends."

## Edit 13 — edit website `06d-alerting/index.md` (companion slide, website repo)

**Before**: "Walk through it: **Alert activity** (alerts that are firing), one open rule, and the condition it watches."

**After**: "Walk through it: **Alert activity** (alerts that are firing), one open rule, the condition it watches, and where its notifications go."

## Addendum — follow-up edits (same session, user-directed)

- **Edit `blocks[0]`** (twice): dropped the "**What you will learn:**" label; rewrote the scenario concretely (checkout response time, disk space examples → the pain → the alert rule definition → bridge to Play, including the honesty clause "the number it watches is made up, but the watching is real"); then extended the promise with "You'll start where rules are defined".
- **Add `blocks[1]`–`blocks[3]`** (shifting later paths by 3): new one-step section `where-rules-are-defined` ("See where rules are defined") with a `navigate` to `/alerting/list` plus intro/summary bookends. Verified live: page loads for anonymous users with h1 "Alert rules"; **no New alert rule button for visitors**, so copy says "With edit permissions, this is also where you'd create a new one; Play visitors can look but not touch." No highlight step — the rule list is virtualised (`load-more-rule-groups`), and a navigate-only step avoids rule 21 entirely.
- **Edit closer**: now opens "A rule is defined once, on **Alert rules**."
- **Edit `manifest.json`**: `startingLocation` → `/alerting/list`; description mentions the definitions page.
- **Edit website slide `06d-alerting/index.md`**: walk-through sentence now starts with "where rules are defined".
- **Live research** (rules API, anonymous): 105 rules on Play, 11 firing at check time. `Random Single Serie` is synthetic (testdata `random_walk`, 0–100, threshold 75). Others watch real running demo services: `QuickPizza Memory Utilization Outliers` (real per-pod memory), `CheckoutServiceP95Latency` (real p95 from RPC histograms of a live checkout service). Guide copy unchanged by this — our rule's honesty clause stands.

## Unchanged paths

`blocks[3]` (post-section-1 bookend) — byte-identical. Section ids, guide id, title, all reftargets/verify/objectives on pre-existing steps, `manifest.json` targeting/startingLocation.
