{
  "id": "saved-queries-panel-edit",
  "title": "Save & reuse queries",
  "blocks": [
    {
      "type": "markdown",
      "content": "Saved queries lets you save a query for later and pull those queries back into the editor. This guide helps you both save and reintroduce saved queries while editing a dashboard panel. These can also be used in other features, like Explore!"
    },
    {
      "type": "markdown",
      "content": "First, open a panel and select **Edit** to open the query editor. The **Save** and **Replace** options live in the query header, next to your query. The \"Add saved query\" action lives in the \"+\" menu next to \"Queries &amp; Expressions\" on the left."
    },
    {
      "type": "markdown",
      "content": "To save the query you're working on, complete these steps:"
    },
    {
      "type": "section",
      "title": "Save a query",
      "requirements": [
        "has-feature:queryLibrary"
      ],
      "blocks": [
        {
          "type": "guided",
          "content": "Select the \"Save\" button in the query header to open the save dialog. You can then name your query, and add a description and tags. Confirm the save once ready!",
          "steps": [
            {
              "action": "highlight",
              "reftarget": "button[data-testid='data-testid save to query library button']"
            }
          ],
          "stepTimeout": 30000
        }
      ]
    },
    {
      "type": "markdown",
      "content": "Prefer to pull in a query you saved earlier? Replace the query in the current row with a saved one:"
    },
    {
      "type": "section",
      "title": "Replace with a saved query",
      "requirements": [
        "has-feature:queryLibrary"
      ],
      "blocks": [
        {
          "type": "guided",
          "content": "Select the \"Replace\" button in the query header. Inside the Saved queries modal, search and filter for your desired query and then confirm selection.",
          "steps": [
            {
              "action": "highlight",
              "reftarget": "button[data-testid='data-testid replace query from library button']"
            }
          ],
          "stepTimeout": 30000
        }
      ]
    },
    {
      "type": "section",
      "title": "Add from Saved queries",
      "requirements": [
        "has-feature:queryLibrary"
      ],
      "blocks": [
        {
          "type": "guided",
          "content": "Click the \"+\" button to open the add query menu. Select the \"Add saved query\" option to open the Saved queries modal. Then, filter and search for your desired query. Confirm selection to add it to the editor!",
          "steps": [
            {
              "action": "highlight",
              "reftarget": "div[data-testid='data-testid Panel editor content'] button[aria-label='Add query or expression']"
            },
            {
              "action": "highlight",
              "reftarget": "div[data-testid='data-testid portal-container'] [role='menuitem']:text('Add saved query')"
            }
          ],
          "stepTimeout": 30000
        }
      ]
    }
  ]
}
