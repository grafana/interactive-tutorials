{
  "id": "saved-queries-panel-edit",
  "title": "Save & reuse queries",
  "blocks": [
    {
      "type": "markdown",
      "content": "Saved queries lets you save a query for later and pull those queries back into the editor. This guide helps you both save and reintroduce saved queries while editing a dashboard panel. These can also be used in other features, like Explore!"
    },
    {
      "type": "markdown",
      "content": "First, open a panel and select **Edit** to open the query editor. The **Save** and **Replace** options live in the query header, next to your query. **Add saved query** lives in the **+** menu next to **Queries & Expressions** on the left."
    },
    {
      "type": "markdown",
      "content": "To save the query you're working on, complete these steps:"
    },
    {
      "type": "section",
      "id": "save-a-query",
      "title": "Save a query",
      "requirements": [
        "has-feature:queryLibrary"
      ],
      "blocks": [
        {
          "type": "guided",
          "content": "Select **Save** in the query header to open the save dialog. Give your query a name (and an optional description and tags), then confirm to save it to the library.",
          "skippable": true,
          "steps": [
            {
              "action": "highlight",
              "reftarget": "button[data-testid='data-testid save to query library button']",
              "description": "Click **Save**, then complete the dialog to finish saving.",
              "requirements": [
                "exists-reftarget"
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "markdown",
      "content": "Your query is now saved to the library and ready to reuse in any panel."
    },
    {
      "type": "markdown",
      "content": "Prefer to pull in a query you saved earlier? Replace the query in the current row with a saved one:"
    },
    {
      "type": "section",
      "id": "replace-with-saved-query",
      "title": "Replace with a saved query",
      "requirements": [
        "has-feature:queryLibrary"
      ],
      "blocks": [
        {
          "type": "guided",
          "content": "Select **Replace** in the query header. In the Saved queries dialog, search or filter for the query you want, then select it to replace the current query.",
          "skippable": true,
          "steps": [
            {
              "action": "highlight",
              "reftarget": "button[data-testid='data-testid replace query from library button']",
              "description": "Click **Replace**, then pick a query in the dialog.",
              "requirements": [
                "exists-reftarget"
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "markdown",
      "content": "The current row now runs your chosen saved query."
    },
    {
      "type": "markdown",
      "content": "You can also bring a saved query into the panel as a brand-new query:"
    },
    {
      "type": "section",
      "id": "add-from-saved-queries",
      "title": "Add from Saved queries",
      "requirements": [
        "has-feature:queryLibrary"
      ],
      "blocks": [
        {
          "type": "guided",
          "content": "Click **+** to open the add query menu, then choose **Add saved query** to open the Saved queries dialog. Search or filter for the query you want, then select it to add it to the editor.",
          "skippable": true,
          "steps": [
            {
              "action": "highlight",
              "reftarget": "div[data-testid='data-testid Panel editor content'] button[aria-label='Add query or expression']",
              "description": "Click **+** to open the add query menu.",
              "requirements": [
                "exists-reftarget"
              ]
            },
            {
              "action": "highlight",
              "reftarget": "div[data-testid='data-testid portal-container'] [role='menuitem']:text('Add saved query')",
              "description": "Click **Add saved query**, then pick a query in the dialog.",
              "requirements": [
                "exists-reftarget"
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "markdown",
      "content": "The saved query is now added to the panel. You've saved and reused queries in the panel editor!"
    }
  ]
}
