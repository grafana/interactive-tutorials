---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-07T00:00:00Z"
---

# Migration Notes: grafana-cloud-tour-lj

## Files Created

- `manifest.json` -- path-level manifest (type: path, 6 milestones)
- `content.json` -- path-level cover page derived from website _index.md
- `business-value/manifest.json` -- step 1 manifest
- `explore-send-data/manifest.json` -- step 2 manifest
- `explore-connect-data/manifest.json` -- step 3 manifest
- `explore-platform-components/manifest.json` -- step 4 manifest
- `explore-dashboards/manifest.json` -- step 5 manifest
- `end-journey/manifest.json` -- step 6 manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` (path) | directory name | `grafana-cloud-tour-lj` |
| `type` (path) | convention | `path` |
| `description` (path) | recommender rule (navigation-cloud.json) | "Step-by-step guide: Tour Grafana Cloud capabilities." |
| `category` | website _index.md `journey.group` | `onboarding` |
| `author.name` | CODEOWNERS + git log | `BeverlyJaneJ` |
| `author.team` | convention (learning path) | `Grafana Documentation` |
| `startingLocation` | recommender match (first urlPrefixIn entry) | `/a/cloud-home-app` |
| `targeting.match` | recommender rule (navigation-cloud.json) | `{"and": [{"urlPrefixIn": ["/a/cloud-home-app", "/a/grafana-setupguide-app/getting-started"]}, {"targetPlatform": "cloud"}]}` |
| `testEnvironment.tier` | recommender match (targetPlatform: cloud) | `cloud` |
| `milestones` | website step weights | 6 steps ordered by weight |
| `suggests` (path) | end-journey `related_journeys` | `visualization-metrics-lj`, `linux-server-integration-lj` |
| Step `description` values | website step index.md frontmatter | per-step (see below) |
| Step `depends`/`recommends` | weight ordering | sequential chain |

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| navigation-cloud.json | `https://grafana.com/docs/learning-paths/grafana-cloud-tour/` | `{"and": [{"urlPrefixIn": ["/a/cloud-home-app", "/a/grafana-setupguide-app/getting-started"]}, {"targetPlatform": "cloud"}]}` |

## Data Quality Issues

- No entry found for grafana-cloud-tour in journeys.yaml -- this path is not registered in the cross-journey graph.
- All 6 steps had `pathfinder_data` frontmatter; no directory name fallback was needed.
- The `_index.md` frontmatter has no `journey.links.to` field, so path-level `recommends` is empty. Related journeys from the end-journey step were placed in `suggests` at the path level.
- The end-journey `related_journeys` heading says "Consider taking the following paths after you complete this path" -- this is a suggestion, not a prerequisite, confirming `suggests` is correct.
- Side journeys on steps (explore-send-data, explore-connect-data, explore-platform-components, explore-dashboards) link to external Grafana Cloud docs, not to learning paths in this repo, so they are not mappable to package IDs.

## Dangling References

- `visualization-metrics-lj` -- directory exists in repo (not yet migrated)
- `linux-server-integration-lj` -- directory exists in repo (not yet migrated)

(Dangling references are expected and preferred -- the Pathfinder CLI catches them during validation.)

## Validation Results

- Path-level package: PASS (no errors, info-level defaults applied)
- All 6 step packages: PASS (no errors; expected warnings for missing targeting/startingLocation on steps -- steps inherit targeting from path level)

## Surprises / Notes

- The _index.md `step: 1` field at path level is inconsistent with step numbering in child steps (which start at step: 2). This appears to be a website convention where the path overview page counts as step 1.
- No index.json rules exist for this learning path (as expected -- LJ routing is in the recommender).
