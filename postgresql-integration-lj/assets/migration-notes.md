---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-08T00:00:00Z"
status: complete
---

# Migration Notes: postgresql-integration-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 9 milestones, targeting from recommender)
- `content.json` — path-level cover page derived from website `_index.md`
- `business-value/manifest.json` — step 1 manifest
- `select-platform/manifest.json` — step 2 manifest
- `install-alloy/manifest.json` — step 3 manifest
- `prepare-configuration/manifest.json` — step 4 manifest
- `configure-alloy/manifest.json` — step 5 manifest
- `test-connection/manifest.json` — step 6 manifest
- `install-dashboards-alerts/manifest.json` — step 7 manifest
- `understand-dashboards/manifest.json` — step 8 manifest
- `end-journey/manifest.json` — step 9 manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| id | directory name | `postgresql-integration-lj` |
| type | directory convention (`*-lj`) | `path` |
| description | recommender rule (connections-cloud.json) | "Step-by-step guide: Monitor PostgreSQL with Grafana Alloy." |
| category | website `_index.md` `journey.group` | `data-availability` |
| author.team | learning path convention | `Grafana Documentation` |
| author.name | CODEOWNERS (`/postgresql-integration-lj/`) | `chri2547` |
| startingLocation | first URL from recommender match (`urlPrefixIn[0]`) | `/connections/add-new-connection/postgres` |
| targeting.match | recommender rule (connections-cloud.json) | `and[or[urlPrefixIn, urlRegex], targetPlatform:cloud]` |
| testEnvironment.tier | match contains `targetPlatform: cloud` | `cloud` |
| milestones | website step `weight` ordering | 9 steps in weight order |
| recommends | website `_index.md` `journey.links.to` | `drilldown-metrics-lj`, `drilldown-logs-lj` |
| suggests (path) | empty — related_journeys overlap with links.to | `[]` |
| step descriptions | website step `index.md` `description` frontmatter | all 9 steps sourced from website |

## Content Removed During Migration

- Removed image: `![Example PostgreSQL overview dashboard showing database performance metrics](/media/integrations/postgres/screenshots/postgres-overview.png)` — website-relative image path
- Removed prerequisite: "A Grafana Cloud account. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)." — redundant for Pathfinder users

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| connections-cloud.json | `https://grafana.com/docs/learning-paths/postgresql-integration/` | `{"and": [{"or": [{"urlPrefixIn": ["/connections/add-new-connection/postgres", "/connections/infrastructure"]}, {"urlRegex": "/connections/add-new-connection/?$"}]}, {"targetPlatform": "cloud"}]}` |

No rules found in connections-oss.json for this path. Only one recommender rule exists (cloud only).

## Data Quality Issues

- **journeys.yaml vs _index.md mismatch**: journeys.yaml uses ID `monitor-postgreSQL` with `links.to: metrics-drilldown` and what appears to be `id: logs-drilldown` (likely a YAML key error — should be `to: logs-drilldown`). The `_index.md` uses `links.to: drilldown-metrics` and `links.to: drilldown-logs`. These IDs differ: `metrics-drilldown` (journeys.yaml) vs `drilldown-metrics` (_index.md), and `logs-drilldown` (journeys.yaml) vs `drilldown-logs` (_index.md). Used `_index.md` values as authoritative since they map to actual directory names (`drilldown-metrics-lj`, `drilldown-logs-lj`).
- **end-journey related_journeys overlap with _index.md links.to**: Both point to drilldown-metrics and drilldown-logs. Used `_index.md` `links.to` for path-level `recommends` and end-journey `related_journeys` for step-level `suggests`.
- All 9 steps had `pathfinder_data` frontmatter in website markdown — no directory-name-only fallback was needed.

## Validation Results

- `validate --package postgresql-integration-lj`: PASS (info: defaults applied for repository, language, schemaVersion, conflicts, replaces)
- All 9 step packages: PASS (each has expected warnings for missing targeting and startingLocation, which are inherited from path level)

## Surprises / Notes

- The `configure-alloy` and `install-alloy` steps have `side_journeys` in website markdown, but all links are external (docs pages, YouTube, GitHub) — none match the `/docs/learning-paths/<name>/` pattern, so no `suggests` entries were derived from them.
- The recommender has only a cloud targeting rule (connections-cloud.json). No OSS rule exists for this path.
