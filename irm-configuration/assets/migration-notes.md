---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: irm-configuration

## Files Created

- `manifest.json` — standalone guide manifest derived from content.json id/title, matching index.json rule, and git history.

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `irm-configuration` |
| `type` | constant | `guide` |
| `description` | `index.json` rule (verbatim) | "Hands-on guide: This tutorial guides you through the process of setting up Grafana IRM for on-call notifications, including on-call schedules, escalation chains, and integrations." |
| `category` | fallback (not in a *-lj directory) | `general` |
| `author.team` | constant (standalone guide) | `interactive-learning` |
| `author.name` | git log (non-bot authors) | `David Allen, Tom Glenn` |
| `startingLocation` | first URL-bearing leaf of `match` expression | `/a/grafana-irm-app` |
| `targeting.match` | `index.json` rule (verbatim copy) | `or: [urlPrefixIn: [...]]` |
| `testEnvironment.tier` | tier inference: match exists, no `source`, no `targetPlatform: cloud` → local | `local` |
| `depends` / `recommends` / `suggests` / `provides` | no data available | `[]` |

## Validator Result

```
✅ irm-configuration (irm-configuration)
  INFO: "repository" not specified, defaulting to "interactive-tutorials"
  INFO: "language" not specified, defaulting to "en"
  INFO: "schemaVersion" not specified, defaulting to "1.1.0"
  INFO: "conflicts" not specified, defaulting to []
  INFO: "replaces" not specified, defaulting to []
```

All INFO messages are expected — omitting `repository`, `language`, `schemaVersion`, `conflicts`, and `replaces` is correct (schema defaults apply).

## Surprises / Notes

- The CLI path referenced in the skill (`dist/cli/cli/index.js` relative to the repo root) does not exist in this repo. The validator was found and run from `/Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js`.
- `testEnvironment.tier` is `local` because the match expression contains only `urlPrefixIn` predicates — no `source` rule and no `targetPlatform: "cloud"`. This means the guide is expected to work on any locally-running Grafana instance with the IRM app installed, not just Grafana Cloud. Flag for manual review if Cloud-only targeting is intended.
