---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: connect-prometheus-metrics

## Files Created

- `manifest.json` — standalone guide manifest; no index.json rule found, so targeting and startingLocation are omitted and description was synthesized from content.json.

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `connect-prometheus-metrics` |
| `type` | fixed | `"guide"` |
| `description` | synthesized from `content.json` title + intro | `"Hands-on guide: Learn how to connect Prometheus metrics to Grafana Cloud using the Connections catalog and Hosted Prometheus."` |
| `category` | default (not inside a `*-lj` directory) | `"general"` |
| `author.team` | convention (standalone guide) | `"interactive-learning"` |
| `author.name` | git log (`tacole02@gmail.com`) | `"tacole02"` |
| `startingLocation` | omitted — no index.json rule | — |
| `targeting` | omitted — no index.json rule | — |
| `testEnvironment.tier` | default (no match expression) | `"cloud"` |
| `depends` | no data | `[]` |
| `recommends` | no data | `[]` |
| `suggests` | no data | `[]` |
| `provides` | no data | `[]` |

## No Targeting — Expected

No `index.json` rule exists for `connect-prometheus-metrics`. This is not a migration error — targeting was simply not available to migrate. The closest rule (`connect-metrics-data`, "Connect a metrics data source to Grafana Cloud") has a different URL segment and is a distinct guide. `targeting` and `startingLocation` are correctly omitted from the manifest.

## Flags for Manual Review

- **`description` was synthesized** (not taken from an index.json rule). Recommend manual review to ensure it accurately represents the guide in a course catalog context.
- **`author.name`** was inferred from the git committer email (`tacole02@gmail.com` → handle `tacole02`). Verify this is the correct GitHub handle.

## Validator Result

CLI unavailable — blocker. The `dist/cli/cli/index.js` file does not exist in the `grafana-pathfinder-app` checkout at `/Users/davidallen/hax/grafana-pathfinder-app`. The CLI source is present at `src/cli/` but has not been built. Run `npm run build` (or equivalent) in the pathfinder-app repo to build the CLI, then re-run:

```bash
node /Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js validate --package /Users/davidallen/hax/interactive-tutorials/connect-prometheus-metrics
```

## Surprises / Notes

- The guide `connect-prometheus-metrics` does not have its own index.json rule. There is a separate guide `connect-metrics-data` whose URL segment is different. These may be related or may serve different purposes; the relationship is worth clarifying.
- The guide directory contained only `content.json` at migration time — no pre-existing `assets/` directory or other files.
- `content.json` SHA-256 confirmed unchanged after all writes: `c157e9bd014efffdeee1eeb1cf263628350a5cf79d419516901b53251e63717d`.

## TODO

- [ ] Determine whether `connect-prometheus-metrics` should have a `targeting` rule in `index.json` (e.g., matching `/connections` pages on cloud). If yes, add an index.json rule and re-run migration.
- [ ] Verify `author.name: "tacole02"` is the correct GitHub handle for Taylor Cole.
- [ ] Build the pathfinder-app CLI (`npm run build` in `grafana-pathfinder-app`) and run `validate --package` to confirm the manifest passes schema validation.
- [ ] Consider whether `connect-prometheus-metrics` and `connect-metrics-data` serve distinct purposes or if one supersedes the other.
