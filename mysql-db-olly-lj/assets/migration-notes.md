---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-14T00:00:00Z"
status: incomplete
---

# Migration Notes: mysql-db-olly-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 7 milestones, targeting from recommender)
- `content.json` — path-level cover page (content from former `welcome` step, with Grafana Cloud account prerequisite removed)
- `business-value/manifest.json` — step 1 manifest
- `check-requirements/manifest.json` — step 2 manifest
- `prepare-mysql/manifest.json` — step 3 manifest
- `configure-alloy/manifest.json` — step 4 manifest
- `verify-telemetry/manifest.json` — step 5 manifest
- `explore-queries/manifest.json` — step 6 manifest
- `end-journey/manifest.json` — step 7 manifest

The `welcome/` directory was removed entirely — its content now lives in the path-level `content.json`.

## Field Derivation Summary

### Path-level manifest

| Field | Source | Value |
|-------|--------|-------|
| `id` | Directory name | `mysql-db-olly-lj` |
| `type` | Convention | `path` |
| `description` | Recommender rule (`observability-cloud.json`) | "Step-by-step guide: Set up Grafana Cloud Database Observability for MySQL to monitor query performance, explain plans, and wait events." |
| `category` | Website `_index.md` `journey.group` | `data-availability` |
| `author.team` | Learning path convention | `Grafana Documentation` |
| `author.name` | Git history (Chris Moyer) cross-referenced with CODEOWNERS patterns for similar MySQL paths | `chri2547` |
| `startingLocation` | First URL from recommender match expression | `/a/grafana-dbo11y-app` |
| `targeting.match` | Recommender rule (`observability-cloud.json`) | `and: [urlPrefixIn: ["/a/grafana-dbo11y-app", "/a/grafana-dbo11y-app/configuration"], targetPlatform: "cloud"]` |
| `testEnvironment.tier` | Match contains `targetPlatform: "cloud"` | `cloud` |
| `milestones` | Website markdown step numbers (steps 2-8; welcome merged into path-level content.json) | 7 steps in order |
| `recommends` | `_index.md` has no `journey.links.to` | `[]` |
| `suggests` | `end-journey` `related_journeys` | `["drilldown-metrics-lj", "drilldown-logs-lj", "mysql-integration-lj"]` |

### Step manifests

| Step | Description Source |
|------|-------------------|
| `business-value` | Website `business-value/index.md` description |
| `check-requirements` | Website `check-requirements/index.md` description |
| `prepare-mysql` | Website `prepare-mysql/index.md` description |
| `configure-alloy` | Website `configure-alloy/index.md` description |
| `verify-telemetry` | Website `verify-telemetry/index.md` description |
| `explore-queries` | Website `explore-queries/index.md` description |
| `end-journey` | Website `end-journey/index.md` description |

### Path-level content.json

Content was copied from the `welcome` step's `content.json` into the path-level `content.json` (with id `mysql-db-olly-lj`). The "Grafana Cloud account" prerequisite was removed per Pathfinder conventions (users are already in Grafana). The `welcome/content.json` was **not modified** — it retains the original content.

## Content Removed During Migration

- Removed prerequisite: `"A Grafana Cloud account with Editor or Admin role. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)."` — redundant for Pathfinder users who are already in Grafana Cloud.

## Flags for Manual Review

- **`welcome` directory removed** — its content was merged into the path-level `content.json`. The `business-value` step is now the first milestone with no `depends`.
- **`welcome` step description** was derived from content.json context rather than a dedicated website step description (no `welcome/index.md` exists in the website; mapped from `_index.md` via `pathfinder_data`).
- **`author.name`** — CODEOWNERS has no specific `/mysql-db-olly-lj/` entry. Used `chri2547` from git history (Chris Moyer) cross-referenced with CODEOWNERS entries for `/mysql-data-source-lj/` and `/mysql-integration-lj/`. Confirm this is correct.
- **`validate --package` was not run** — the Pathfinder CLI is not built (`node_modules` and `dist/` are missing in the `grafana-pathfinder-app` checkout). Build the CLI and run validation manually.

## Dangling References

No dangling references detected. All `suggests` targets exist as directories in the interactive-tutorials repo:
- `drilldown-metrics-lj` — exists
- `drilldown-logs-lj` — exists
- `mysql-integration-lj` — exists

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| `observability-cloud.json` | `https://grafana.com/docs/learning-paths/mysql-db-olly/` | `and: [urlPrefixIn: ["/a/grafana-dbo11y-app", "/a/grafana-dbo11y-app/configuration"], targetPlatform: "cloud"]` |

Only one recommender rule was found for this learning path.

## Data Quality Issues

- **No `journeys.yaml` entry** — the `mysql-db-olly` learning path has no entry in `journeys.yaml`. The nearest entries are `monitor-mysql` and `connect-mysql`, which map to different learning paths (`mysql-integration-lj` and `mysql-data-source-lj`). The website team should add a `journeys.yaml` entry for `mysql-db-olly` when the learning path is published.
- **Website markdown on unmerged branch** — the website markdown for this learning path exists only on the `docs/db-olly-course` branch (not yet merged to `main`). All website-derived fields were read from this branch. If the branch is updated before merge, re-check field values.
- **Step ordering** — all steps mapped via `pathfinder_data` frontmatter in the website markdown. The `welcome` step mapped from `_index.md` (pathfinder_data: `mysql-db-olly-lj/welcome`), not a separate step directory.
- **Side journeys** — `configure-alloy`, `prepare-mysql`, and `explore-queries` have `side_journeys` in their website markdown, but all links point to Grafana docs pages (not learning path URLs), so no `suggests` were derived at the step level.

## TODO

- [ ] TODO(review): Build the Pathfinder CLI and run `validate --package` on `mysql-db-olly-lj/` and each step directory
- [ ] TODO(review): Confirm `author.name: chri2547` is the correct attribution for this learning path
- [ ] TODO(review): Add a `journeys.yaml` entry for `mysql-db-olly` when the website learning path is published
