---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: k8s-mem

## Files Created

- `manifest.json` — standalone guide manifest derived from index.json rule and git history

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `k8s-mem` |
| `type` | Fixed | `"guide"` |
| `description` | `index.json` rule (verbatim) | "Hands-on guide: Explore memory usage in Kubernetes Monitoring, from namespaces to containers." |
| `category` | Default (not inside a `*-lj` directory) | `"general"` |
| `author.team` | Convention (standalone guide) | `"interactive-learning"` |
| `author.name` | `git log --follow` on `content.json` | `"David Allen, Tom Glenn"` |
| `startingLocation` | First URL-bearing leaf in `match` expression | `"/a/grafana-k8s-app"` |
| `targeting.match` | `index.json` rule (verbatim copy) | `and: [urlPrefix /a/grafana-k8s-app, source play.grafana.org]` |
| `testEnvironment.tier` | `source: "play.grafana.org"` in match (concrete hostname → cloud) | `"cloud"` |
| `testEnvironment.instance` | `source` value in match | `"play.grafana.org"` |
| `depends` / `recommends` / `suggests` / `provides` | No data available | `[]` |

## Validator Result

Command: `node dist/cli/cli/index.js validate --package k8s-mem`

Result: PASS

```
✅ k8s-mem (k8s-mem)
  ℹ️  INFO: manifest.json: "repository" not specified, defaulting to "interactive-tutorials"
  ℹ️  INFO: manifest.json: "language" not specified, defaulting to "en"
  ℹ️  INFO: manifest.json: "schemaVersion" not specified, defaulting to "1.1.0"
  ℹ️  INFO: manifest.json: "conflicts" not specified, defaulting to []
  ℹ️  INFO: manifest.json: "replaces" not specified, defaulting to []
```

All INFOs are expected schema defaults. No warnings or errors.

## Surprises / Notes

- The `k8s-cpu` and `k8s-mem` guides share identical `match` expressions in `index.json`. This is intentional — both guides cover different aspects of the same product (CPU vs memory usage in Kubernetes Monitoring) and are designed to surface together in the same context.
- `"play"` is not a valid tier value. play.grafana.org is a specific cloud instance, correctly modelled as `tier: "cloud"` + `instance: "play.grafana.org"`.

## TODO

- [ ] Add `author.name` for BeverlyJaneJ if she contributed to `k8s-mem` (not found in git history for this file, unlike `k8s-cpu`).
