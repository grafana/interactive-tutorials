---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: sm-setting-up-your-first-check

## Files Created

- `manifest.json` — standalone guide manifest derived from content.json id/title, index.json rule, and git history

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `sm-setting-up-your-first-check` |
| `type` | Fixed | `guide` |
| `description` | `index.json` rule (verbatim) | "Hands-on guide: Learn how to create and configure HTTP checks in Grafana Cloud Synthetic Monitoring, including setting up alerts and probe locations." |
| `category` | Default (not inside a `*-lj` directory) | `general` |
| `author.team` | Default for standalone guide | `interactive-learning` |
| `author.name` | `git log --follow` (bots excluded) | `David Allen, Jayclifford345` |
| `startingLocation` | First URL-bearing leaf in `match` (depth-first) | `/a/grafana-synthetic-monitoring-app` |
| `targeting.match` | `index.json` rule (verbatim) | `{ "and": [{ "targetPlatform": "cloud" }, { "urlPrefix": "..." }] }` |
| `testEnvironment.tier` | Tier inference: match has `targetPlatform: "cloud"` but no `source` | `cloud` |

## Validator Result

`node dist/cli/cli/index.js validate --package sm-setting-up-your-first-check` — PASSED (✅)

INFO messages (expected, non-blocking):
- `repository` not specified, defaulting to `"interactive-tutorials"` (correctly omitted per schema default)
- `language` not specified, defaulting to `"en"` (correctly omitted per schema default)
- `schemaVersion` not specified, defaulting to `"1.1.0"`
- `conflicts` not specified, defaulting to `[]`
- `replaces` not specified, defaulting to `[]`

## Surprises / Notes

- `Cursor Agent <cursoragent@cursor.com>` appeared in git history and was excluded as an automation author.
- The `testEnvironment` tier is `cloud` (not `local`) because the match expression contains `targetPlatform: "cloud"`, even though no `source` predicate is present. No `instance` field is set (correct — `instance` is only set when a `source` value is available).
- The CLI was not found at `dist/cli/cli/index.js` within the `interactive-tutorials` repo; it was found at `/Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js`.
