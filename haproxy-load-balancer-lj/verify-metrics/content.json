{
  "schemaVersion": "1.0.0",
  "id": "haproxy-load-balancer-verify-metrics",
  "title": "Verify metrics appear in Grafana Cloud",
  "blocks": [
    {
      "type": "markdown",
      "content": "After configuring the integration, verify that HAProxy metrics are flowing into Grafana Cloud. This step confirms that Alloy is scraping the stats endpoint and successfully forwarding data."
    },
    {
      "type": "section",
      "autoCollapse": false,
      "blocks": [
        {
          "type": "interactive",
          "action": "highlight",
          "reftarget": "a[data-testid='data-testid Nav menu item'][href='/explore']",
          "content": "Click **Explore** in the left-side menu.",
          "requirements": ["navmenu-open"]
        },
        {
          "type": "interactive",
          "action": "highlight",
          "reftarget": "[data-testid='data-testid Select a data source']",
          "content": "Select your **Prometheus** data source from the data source picker.",
          "requirements": ["on-page:/explore"],
          "doIt": false
        },
        {
          "type": "interactive",
          "action": "highlight",
          "reftarget": "[data-testid='data-testid metric select']",
          "content": "In the **Metric** field, type `haproxy_frontend_http_requests_total` and select it from the dropdown.",
          "requirements": ["on-page:/explore"],
          "doIt": false
        },
        {
          "type": "interactive",
          "action": "highlight",
          "reftarget": "button[data-testid='data-testid RefreshPicker run button']",
          "content": "Click **Run query**. If data appears, HAProxy metrics are being collected successfully.",
          "requirements": ["on-page:/explore"]
        },
        {
          "type": "interactive",
          "action": "noop",
          "content": "If no data appears, wait 2-3 minutes for the first scrape to complete, then try again.\n\nYou can also try `up{job=~\".*haproxy.*\"}` to confirm the scrape target is healthy. A value of `1` means the target is up and being scraped."
        }
      ]
    }
  ]
}
