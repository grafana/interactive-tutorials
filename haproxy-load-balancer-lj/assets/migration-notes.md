---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-09T00:00:00Z"
status: incomplete
---

# Migration Notes: haproxy-load-balancer-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 9 milestones)
- `content.json` — path-level cover page derived from `welcome/content.json`
- `business-value/manifest.json` — step 1 manifest
- `select-platform/manifest.json` — step 2 manifest
- `configure-haproxy-stats/manifest.json` — step 3 manifest
- `install-alloy/manifest.json` — step 4 manifest
- `configure-integration/manifest.json` — step 5 manifest
- `verify-metrics/manifest.json` — step 6 manifest
- `install-dashboards/manifest.json` — step 7 manifest
- `install-alerts/manifest.json` — step 8 manifest
- `end-journey/manifest.json` — step 9 manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` (path) | directory name | `haproxy-load-balancer-lj` |
| `type` (path) | convention | `path` |
| `description` (path) | summarized from welcome step content.json | Step-by-step guide: Monitor HAProxy load balancers using Grafana Cloud with Alloy, prebuilt dashboards, and alerting. |
| `category` | fallback (no website markdown); inferred from content topic and linux-server-integration-lj precedent | `data-availability` |
| `author.name` | git log | Chris Moyer |
| `author.team` | convention (learning path) | Grafana Documentation |
| `testEnvironment.tier` | fallback (no targeting rules) | `cloud` |
| `targeting` | none found (no recommender rules, no index.json entry) | omitted |
| `startingLocation` | none derivable (no targeting rules) | omitted |
| `milestones` | inferred from content flow (no website weight data) | 9 steps ordered by logical progression |
| `recommends` (path) | no journey.links.to available | `[]` |
| `suggests` (path) | end-journey "Related paths" links resolved to LJ IDs | `["drilldown-metrics-lj", "drilldown-logs-lj", "infrastructure-alerting-lj"]` |
| Step `id` values | each step's content.json | verbatim |
| Step descriptions | summarized from step content.json titles and first-block content | one-line catalog summaries |

## Flags for Manual Review

- **No website markdown found**: No `learning-paths/haproxy-load-balancer/` directory exists in the website repo. All fields that would normally derive from `_index.md` used fallback values.
- **No recommender rules found**: No `learning-journey` type rules matching `haproxy-load-balancer` in the recommender. Targeting and startingLocation omitted.
- **No index.json rule found**: No matching entry in index.json for this learning path.
- **No journeys.yaml entry found**: `haproxy-load-balancer` is not listed in journeys.yaml. No journey links or related journeys available from this source.
- **No CODEOWNERS entry found**: Author derived from git history only.
- **Category inferred**: `data-availability` was inferred from content topic (HAProxy integration setup) and precedent from `linux-server-integration-lj`. Needs verification.
- **Step ordering inferred from content**: No website `weight` data available. Order was determined from logical content flow: business-value → select-platform → configure-haproxy-stats → install-alloy → configure-integration → verify-metrics → install-dashboards → install-alerts → end-journey. Needs verification.
- **Welcome step not included as milestone**: The `welcome/content.json` content was used to create the path-level cover page. The `welcome/` directory was deleted after the cover page was created, matching the convention established by other LJs in the repo.

## Content Removed During Migration

- Removed prerequisite: "A Grafana Cloud account. To create an account, refer to [Grafana Cloud](https://grafana.com/signup/cloud/connect-account)." — redundant for Pathfinder users who are already in Grafana

## Recommender Rules

No `learning-journey` type recommender rules found for this path. The recommender repo was checked at `grafana-recommender/internal/configs/state_recommendations/*.json`. This learning path appears to be newer content not yet registered with the recommender.

## Data Quality Issues

- All steps mapped by directory name only (no `pathfinder_data` available since no website markdown exists).
- This learning path appears to be newer content not yet published to the website learning paths section.
- `end-journey/content.json` contains "Related paths" links that were resolved to `suggests`: `drilldown-metrics-lj`, `drilldown-logs-lj`, `infrastructure-alerting-lj`. All three directories exist in the repo.

## Validation

- Structural validation: passed (IDs match, dependency chain intact, JSON valid)
- Pathfinder CLI (`validate --package`): **not run** — CLI not built (`dist/cli/cli/index.js` does not exist in the grafana-pathfinder-app checkout)

## TODO

- [ ] TODO(review): Verify `category: data-availability` is correct
- [ ] TODO(review): Verify step ordering matches intended learning progression
- [ ] TODO(review): Add website learning path markdown at `content/docs/learning-paths/haproxy-load-balancer/` with proper `_index.md` and step `index.md` files
- [ ] TODO(review): Add recommender `learning-journey` type rules for path-level targeting
- [ ] TODO(review): Add entry to `journeys.yaml` with category and journey links
- [ ] TODO(review): Add `recommends` relationships once related paths are identified
- [ ] TODO(review): Build Pathfinder CLI and run `validate --package` for full validation
