---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-06T00:00:00Z"
---

# Migration Notes: slo-quickstart

## Files Created

- `manifest.json` -- standalone guide manifest for `create-first-slo`

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| id | content.json | `create-first-slo` |
| type | convention | `guide` |
| description | Summarized from content.json intro | Hands-on guide: Create your first SLO using a ratio query |
| category | default (not inside *-lj) | `general` |
| author.team | convention (standalone guide) | `interactive-learning` |
| author.name | git log | `Joe Blubaugh` |
| testEnvironment.tier | default (no match expression) | `cloud` |
| targeting | N/A | Omitted -- no index.json rule found |
| startingLocation | N/A | Omitted -- no match expression to derive from |

## Flags for Manual Review

- No index.json rule exists for this guide. `targeting` and `startingLocation` are omitted. If contextual recommendation is desired, a targeting rule needs to be added manually.
- `description` was summarized from content.json intro prose since no index.json rule description was available. Verify it matches catalog conventions.
- `category` defaulted to `general`. Consider whether a more specific category applies (e.g., `slo`, `observability`).
- CODEOWNERS lists `@grafana/slo-squad` as the owner. Consider whether author.name should reference the team handle instead.

## Validation Result

```
$ node .../cli/index.js validate --package slo-quickstart
PASS: create-first-slo
INFO: repository, language, schemaVersion, conflicts, replaces defaulted
WARN: targeting not specified
WARN: startingLocation not specified, defaulting to "/"
```

All warnings are expected given the absence of an index.json rule.

## Surprises / Notes

- The content.json `id` (`create-first-slo`) differs from the directory name (`slo-quickstart`). This is valid but worth noting.

## TODO

- [ ] Add a targeting rule (index.json or manifest targeting) if contextual recommendation is desired
- [ ] Review whether `category: "general"` should be more specific
- [ ] Review summarized description for catalog accuracy
