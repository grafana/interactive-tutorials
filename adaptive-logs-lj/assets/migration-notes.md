---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-08T00:00:00Z"
status: complete
---

# Migration Notes: adaptive-logs-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 7 milestones, targeting from recommender + index.json)
- `value-adaptive-logs/manifest.json` — step 1 manifest
- `how-it-works/manifest.json` — step 2 manifest
- `review-apply/manifest.json` — step 3 manifest
- `exemptions/manifest.json` — step 4 manifest
- `segmentation/manifest.json` — step 5 manifest
- `monitor-validate/manifest.json` — step 6 manifest
- `end-journey/manifest.json` — step 7 manifest
- `assets/migration-notes.md` — this file

## Files NOT Created

- `content.json` — already existed at path level; verified unchanged by SHA-256

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | directory name | `adaptive-logs-lj` |
| `type` | convention (directory ends with `-lj`) | `path` |
| `description` | recommender rule in `adaptive-logs.json` | "Step-by-step guide: Optimize your logs with Adaptive Logs." |
| `category` | website `_index.md` `journey.group` | `data-availability` |
| `author.team` | convention (learning path) | `Grafana Documentation` |
| `author.name` | CODEOWNERS `/adaptive-logs-lj/` | `chri2547` |
| `startingLocation` | first URL leaf from recommender match | `/a/grafana-adaptivelogs-app` |
| `targeting.match` | recommender `adaptive-logs.json` + `index.json` (combined with `or`) | See manifest |
| `testEnvironment.tier` | `targetPlatform: cloud` in match | `cloud` |
| `milestones` | website step `weight` ordering | 7 steps |

### Step descriptions

All step descriptions were derived from website step `index.md` frontmatter `description` fields (priority 2 — website markdown). No step-level index.json rules exist.

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| `adaptive-logs.json` | `https://grafana.com/docs/learning-paths/optimize-logs-adaptive-logs/` | `{"and": [{"urlPrefix": "/a/grafana-adaptivelogs-app"}, {"targetPlatform": "cloud"}]}` |

Additionally, `index.json` has a matching entry for `adaptive-logs-lj` (line ~286) with match: `{"and": [{"targetPlatform": "cloud"}, {"urlPrefixIn": ["/adaptive-telemetry", "/a/grafana-adaptivelogs-app", "/a/grafana-costmanagementui-app/logs"]}]}`. Both match expressions were combined with a top-level `"or"` in the path manifest targeting.

## Data Quality Issues

- No entry found for `adaptive-logs` in `journeys.yaml`. The `journeys.yaml` file has `optimize-adaptive-metrics` but no corresponding adaptive-logs entry. This means no `links.to` data was available from journeys.yaml (no `recommends` derived from this source).
- All 7 steps had `pathfinder_data` present in website markdown — no directory-name fallback matching was needed.
- Side journeys across steps link to external docs pages (e.g., `/docs/grafana-cloud/adaptive-telemetry/adaptive-logs/...`), not to `/docs/learning-paths/<name>/` patterns. None resolve to package IDs.

## Surprises / Notes

- The root `content.json` already existed in the `adaptive-logs-lj` directory before migration. It was not created or modified.
- The `_index.md` website frontmatter has `step: 1` which is unusual for the path-level index (typically steps start at 2 for value-proposition). The website step numbering starts at step 2 for `value-adaptive-logs`, so there is an implicit "step 1" that is the path cover page itself.
- The recommender `adaptive-logs.json` file also contains several `docs-page` type rules for Adaptive Logs features. Only the `learning-journey` type rule was used for path targeting.
