---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-04-07T00:00:00Z"
status: complete
---

# Migration Notes: linux-server-integration-lj

## Files Created

- `manifest.json` — path-level manifest (type: path, 9 milestones, targeting from recommender)
- `content.json` — path-level cover page derived from website `_index.md`
- `business-value/manifest.json` — step 1 manifest
- `select-platform/manifest.json` — step 2 manifest
- `install-alloy/manifest.json` — step 3 manifest
- `configure-alloy/manifest.json` — step 4 manifest
- `restart-test-connection/manifest.json` — step 5 manifest
- `install-dashboards-alerts/manifest.json` — step 6 manifest
- `understand-dashboards/manifest.json` — step 7 manifest
- `understand-alerts/manifest.json` — step 8 manifest
- `end-linux-server/manifest.json` — step 9 manifest

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | directory name | `linux-server-integration-lj` |
| `type` | convention | `path` |
| `description` | recommender rule (connections-cloud.json) | "Step-by-step guide: Monitor a Linux server in Grafana Cloud." |
| `category` | website `_index.md` `journey.group` | `data-availability` |
| `author.name` | CODEOWNERS (`/linux-server-integration-lj/`) | `chri2547` |
| `author.team` | convention (learning path) | `Grafana Documentation` |
| `startingLocation` | recommender match expression (first URL leaf) | `/connections/add-new-connection/linux-node` |
| `targeting.match` | recommender `connections-cloud.json` | `and` with `urlPrefixIn` + `urlRegex` + `targetPlatform: cloud` |
| `testEnvironment.tier` | recommender match (`targetPlatform: cloud`) | `cloud` |
| `milestones` | website step `weight` ordering | 9 steps in weight order |
| `recommends` | website `_index.md` `journey.links.to` | `drilldown-metrics-lj`, `drilldown-logs-lj`, `fleet-mgt-monitor-health-lj` |

Step descriptions were derived from website step `index.md` frontmatter `description` fields (condensed to one line where needed).

## Recommender Rules

| Source File | Rule URL | Match Expression |
|-------------|----------|------------------|
| `connections-cloud.json` | `https://grafana.com/docs/learning-journeys/linux-server-integration/` | `{"and": [{"or": [{"urlPrefixIn": ["/connections/add-new-connection/linux-node", "/connections/infrastructure"]}, {"urlRegex": "/connections/add-new-connection/?$"}]}, {"targetPlatform": "cloud"}]}` |

## Dangling References

- `fleet-mgt-monitor-health-lj` — referenced in path `recommends` and step `end-linux-server` `suggests`; directory does not exist in this repo. This is expected; the CLI will catch it during repository-level validation.

## Data Quality Issues

- **journeys.yaml vs _index.md mismatch**: journeys.yaml entry `monitor-linux` uses link IDs `metrics-drilldown`, `logs-drilldown`, `monitor-collector-health`. The `_index.md` `journey.links.to` uses `drilldown-metrics`, `drilldown-logs`, `fleet-mgt-monitor-health`. The `_index.md` values were used as authoritative (they map to actual directory names in this repo as `drilldown-metrics-lj`, `drilldown-logs-lj`, `fleet-mgt-monitor-health-lj`). The website team should reconcile the naming inconsistency between journeys.yaml and _index.md.
- All 9 steps had `pathfinder_data` in their website markdown except `end-linux-server`. The `end-linux-server` step was mapped by directory name matching (confirmed: directory exists with content.json).
- Step `configure-alloy` has no `description` in its website frontmatter. Description was derived from the frontmatter `description` field which reads: "Learn to configure the Linux server integration in Grafana Cloud".

## Validation Results

- Path package `linux-server-integration-lj`: PASS (all defaults applied as expected)
- All 9 step packages: PASS (warnings about missing targeting and startingLocation are expected for steps -- targeting lives at the path level)
- All pre-existing content.json files verified unmodified (SHA-256 comparison)
- index.json not modified (read-only access)
- Recommender files not modified (read-only access)
