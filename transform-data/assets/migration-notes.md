---
disclaimer: Auto-generated by migrate-guide. Do not edit manually.
notice: To regenerate, re-run the migration skill on this directory.
migrated_at: "2026-03-09T00:00:00Z"
---

# Migration Notes: transform-data

## Files Created

- `manifest.json` — standalone guide manifest derived from index.json rule and content.json id

## Field Derivation Summary

| Field | Source | Value |
|-------|--------|-------|
| `id` | `content.json` | `find-transformations` |
| `type` | convention | `"guide"` |
| `description` | `index.json` rule | `"Hands-on guide: Learn how to use data transformations in Grafana."` |
| `category` | default (not inside *-lj) | `"general"` |
| `author.team` | convention (standalone guide) | `"interactive-learning"` |
| `startingLocation` | first URL-bearing leaf of `match` (depth-first) | `"/d/"` |
| `targeting.match` | `index.json` rule (copied verbatim) | `{ "and": [{ "urlPrefix": "/d/" }, { "source": ".*\\.grafana\\.net" }] }` |
| `testEnvironment.tier` | `match` contains `source` → cloud | `"cloud"` |
| `testEnvironment.instance` | `source` is a regex (`".*\\.grafana\\.net"`) meaning all Grafana Cloud — no specific instance | omitted |
| `depends` | none available | `[]` |
| `recommends` | none available | `[]` |
| `suggests` | none available | `[]` |
| `provides` | none available | `[]` |

## Flags for Manual Review

- `startingLocation` is derived as `"/d/"` — this is the first URL leaf from the match expression but is a very broad Grafana dashboard prefix, not a specific dashboard URL. Review whether a more specific starting location is appropriate.
- `testEnvironment.instance` is correctly omitted. The `source` predicate `".*\\.grafana\\.net"` is a regex matching all Grafana Cloud instances — it means "any cloud instance", not a specific host, so no `instance` value is set.
- The index.json rule URL uses path segment `find-transformations`, which matches the `content.json` `id`, but the directory is named `transform-data`. The mismatch is noted but harmless — matching is done on `id`, not directory name.

## Validator Output

```
✅ transform-data (find-transformations)
  ℹ️  INFO: manifest.json: "repository" not specified, defaulting to "interactive-tutorials"
  ℹ️  INFO: manifest.json: "language" not specified, defaulting to "en"
  ℹ️  INFO: manifest.json: "schemaVersion" not specified, defaulting to "1.1.0"
  ℹ️  INFO: manifest.json: "conflicts" not specified, defaulting to []
  ℹ️  INFO: manifest.json: "replaces" not specified, defaulting to []
```

Validation: PASS. All INFOs are expected (schema defaults for omitted optional fields).

## Surprises / Notes

- The directory name (`transform-data`) does not match the `content.json` id (`find-transformations`) or the index.json URL path segment (`find-transformations`). This is a naming inconsistency in the repo but does not block migration — the manifest `id` correctly uses `find-transformations` from content.json.
- Git authors for `content.json`: Tom Glenn, Isabel Matwawana. `author.name` was not included in manifest per convention (team field is sufficient for standalone guides; name is optional).
- CLI was not found at `dist/cli/cli/index.js` relative to the repo root. Validation was run from the grafana-pathfinder-app checkout at `/Users/davidallen/hax/grafana-pathfinder-app/dist/cli/cli/index.js`.

## TODO

- [ ] Review whether `startingLocation: "/d/"` is specific enough for this guide's launch context.
